{
  "miniboss_adv_os_5_1_002": {
    "B": "인덱싱을 헷갈리셨네요—`msgs[0]`은 첫 번째 요소인 HumanMessage('안녕')이므로 type은 AIMessage가 아니라 HumanMessage이고, `msgs[1].content`는 두 번째 요소인 AIMessage의 내용인 '반갑습니다'입니다. 리스트의 각 인덱스가 어느 메시지 객체를 가리키는지 차근차근 확인하는 습관을 들이면 이런 실수를 줄일 수 있어요!",
    "C": "첫 번째 print문은 `msgs[0]`의 타입 이름을 출력하므로 'HumanMessage'가 맞지만, 두 번째 print문은 `msgs[1].content`이므로 두 번째 요소인 AIMessage의 content 값인 '반갑습니다'를 출력합니다. 인덱스 번호와 각 메시지 객체의 content 속성을 정확히 매칭하는 연습이 필요해요!",
    "D": "type(msgs[0]).__name__은 msgs[0]이라는 객체의 타입 이름을 문자열로 반환하는 것이지, 문자열 타입 자체인 'str'이 아닙니다. msgs[0]은 HumanMessage 클래스의 인스턴스이므로 그 타입의 이름은 'HumanMessage'가 되는 것이죠. type() 함수는 객체의 클래스를 반환하고, __name__은 그 클래스의 이름을 문자열 형태로 알려주는 속성이라는 것을 기억하면 앞으로 타입 확인 문제를 쉽게 풀 수 있을 거예요!"
  },
  "miniboss_adv_os_5_1_004": {
    "B": "리스트의 모든 메시지를 세는 것이 아니라 isinstance()를 통해 HumanMessage 타입만 필터링하므로, 3개 메시지 중 'HumanMessage(content='안녕')'만 해당되어 len(human_msgs)은 2가 아닌 1입니다. 리스트 컴프리헨션에서 조건을 만족하는 요소만 새로운 리스트에 포함된다는 점을 기억하세요!",
    "C": "리스트 컴프리헨션에서 `isinstance(m, HumanMessage)`로 필터링하면 messages 리스트의 세 개 메시지 중 오직 HumanMessage 타입인 '안녕'만 걸러져서 `human_msgs`의 길이는 1이 맞지만, `messages[0]`은 리스트의 첫 번째 요소인 SystemMessage('너는 튜터야')이므로 타입 이름은 'SystemMessage'입니다. 리스트의 인덱싱은 필터링된 결과가 아닌 원본 messages 리스트에서 접근한다는 점을 기억하세요.",
    "D": "messages 리스트에는 총 3개의 메시지가 있지만, 리스트 컴프리헨션에서 isinstance(m, HumanMessage)로 필터링하면 HumanMessage 타입인 '안녕'만 해당되므로 human_msgs의 길이는 1입니다. messages[0]은 리스트의 첫 번째 요소인 SystemMessage이므로 타입 이름은 'SystemMessage'가 맞지만, 첫 번째 코드 줄의 출력값을 놓치신 것 같습니다. isinstance() 함수를 사용한 타입 필터링과 인덱싱으로 직접 접근하는 것은 다른 결과를 줍니다."
  },
  "miniboss_adv_os_5_1_006": {
    "B": "history 리스트에 추가된 메시지는 HumanMessage, AIMessage, HumanMessage 순서로 총 3개이지만, roles 리스트에는 각 메시지의 타입 이름 문자열만 저장되므로 'HumanMessage'는 첫 번째와 세 번째 위치에만 있어 count 메서드로 셀 때 2개만 반환됩니다. 리스트의 길이와 특정 요소의 개수는 다르다는 점을 기억하세요!",
    "C": "리스트에 추가된 메시지의 순서를 다시 확인해보세요. history에는 HumanMessage('Q1'), AIMessage('A1'), HumanMessage('Q2') 순서로 추가되므로 roles는 ['HumanMessage', 'AIMessage', 'HumanMessage']가 되어야 합니다. 리스트 컴프리헨션은 원본 리스트의 순서를 그대로 유지하면서 각 요소의 타입명을 추출하기 때문에, 요소의 순서가 바뀌면 결과도 달라집니다.",
    "D": "역순으로 리스트를 읽거나 append() 메서드의 동작을 놓치셨을 수 있는데, 코드를 차례대로 따라가면 HumanMessage(Q1) → AIMessage(A1) → HumanMessage(Q2) 순으로 history에 저장되어 ['HumanMessage', 'AIMessage', 'HumanMessage']가 되고, 이 중 'HumanMessage'의 개수는 2개입니다. 리스트 컴프리헨션과 count() 메서드는 입력된 순서를 그대로 유지하므로 요소를 추가한 순서와 그 타입의 개수를 정확히 파악하는 것이 중요합니다."
  },
  "miniboss_adv_os_5_1_009": {
    "B": "함수 내에서 f-string으로 `f'답변: {last[\"content\"]}'`라고 작성했기 때문에 \"답변: \" 부분도 함께 포함되어 출력되는데, 선택지 B는 이 접두사를 빠뜨렸네요. 문자열 포매팅에서는 작성한 텍스트가 모두 그대로 결과에 반영되므로 f-string 안의 모든 내용을 함께 고려해야 합니다.",
    "C": "함수 인자로 전달된 딕셔너리는 `{'role': 'user', 'content': '파이썬이 뭐야?'}`이므로 `last[\"content\"]`는 'role' 키 값인 'user'가 아니라 'content' 키 값인 '파이썬이 뭐야?'를 반환합니다. 딕셔너리에서 대괄호로 접근할 때는 지정한 정확한 키로 값을 가져오는 것이 중요합니다.",
    "D": "`print(response.content)`는 response 객체의 content 속성값을 출력하는데, `fake_invoke` 함수가 `FakeAIMessage` 인스턴스를 반환하고 `__init__`에서 content에 '답변: 파이썬이 뭐야?'를 할당했으므로 None이 아니라 문자열이 출력됩니다. 함수가 명시적으로 return문 없이 끝나거나 객체의 속성이 초기화되지 않았을 때만 None이 되므로, 이 경우처럼 제대로 할당된 속성은 그 값을 가집니다."
  },
  "miniboss_adv_os_5_2_002": {
    "B": "FakeLLM의 invoke 메서드가 받은 메시지 리스트의 마지막 요소인 {'content': 'Hello'}에서 'content' 키의 값 'Hello'만 추출하는 것이 아니라, 전체 딕셔너리를 문자열로 포함시켜 f'Echo: {msgs[-1][\"content\"]}'를 반환하므로 실제 출력값은 'Echo: Hello'입니다. 체인의 각 단계에서 입력값이 어떻게 변환되는지 추적하면서 단계별 입출력을 명확히 파악하는 것이 중요합니다.",
    "C": "FakeLLM의 invoke 메서드에서 msgs[-1]은 리스트의 마지막 요소인 딕셔너리 {'content': 'Hello'}를 가져오고, [\"content\"] 키로 접근하면 'Hello' 문자열만 추출되므로 f-문자열에는 'Echo: Hello'가 저장됩니다. 딕셔너리 전체가 아닌 딕셔너리의 value 값만 f-문자열에 포함되는 점을 기억하세요!",
    "D": "Chain의 invoke 메서드는 각 단계를 순회하면서 result를 업데이트하고 최종 result를 반환하는데, 당신은 반환값이 없다고 생각했을 것 같습니다. 실제로는 FakeLLM().invoke()가 content 속성을 가진 객체를 반환하고, StrParser().invoke()가 그 객체의 content 값인 'Echo: Hello' 문자열을 반환하므로 None이 아닙니다. 메서드가 명시적으로 return 문을 가지고 있으면 그 값을 반환하며, return이 없을 때만 None이 됩니다."
  },
  "miniboss_adv_os_5_2_004": {
    "B": "make_prompt 함수가 실행될 때 kwargs.items()를 통해 role='튜터', question='파이썬이 뭐야?'가 반복 처리되면서 {role}과 {question}이 각각 replace 메서드로 실제 값으로 치환되므로, 템플릿의 중괄호 부분이 그대로 남아있지 않습니다. 문자열 포맷팅은 함수 호출 시점에 즉시 수행되기 때문에 모든 변수가 교체되어야 한다는 점을 기억하면 이 문제를 쉽게 해결할 수 있습니다.",
    "C": "make_prompt 함수는 kwargs.items()로 모든 키-값 쌍을 순회하면서 각각을 치환하는데, role='튜터'로 {role}을 '튜터'로 바꾼 후 question='파이썬이 뭐야?'로 {question}도 함께 치환하기 때문에 {question}이 남아있을 수 없습니다. 딕셔너리의 모든 항목이 순서대로 처리되어 두 개의 플레이스홀더가 모두 실제 값으로 바뀌는 것이 이 함수의 동작 방식입니다.",
    "D": "replace() 메서드는 첫 번째 인자의 모든 occurrence를 바꾸기 때문에, kwargs.items()의 첫 번째 반복에서 {role}이 '튜터'로 완전히 치환되고, 두 번째 반복에서 {question}이 '파이썬이 뭐야?'로 모두 치환되어 보기 D는 두 플레이스홀더 중 하나만 바뀐 중간 상태입니다. 딕셔너리 반복 순서는 순서를 보장하지만, 각 반복마다 모든 해당 패턴이 한 번에 치환되므로 최종 결과는 두 값이 모두 반영됩니다."
  },
  "miniboss_adv_os_5_2_006": {
    "B": "함수 `add_step()`이 호출될 때마다 인자를 순서대로 리스트에 추가하는데, 'prompt', 'llm', 'parser' 순서로 호출되므로 `steps` 리스트에는 이 순서대로 저장되고, 각 변수에 저장된 값도 호출 순서 그대로입니다. 함수의 실행 순서와 리스트에 요소가 추가되는 순서를 구분하여 이해하면 파이프라인의 데이터 흐름을 정확히 파악할 수 있습니다.",
    "C": "add_step() 함수는 호출될 때마다 인자를 steps 리스트에 추가하고 그 값을 반환하는데, prompt → llm → parser 순서로 호출되므로 pipeline 리스트에도 정확히 그 순서대로 저장됩니다. 함수 호출의 실행 순서와 리스트에 저장되는 순서를 일치시키면 이 문제를 쉽게 풀 수 있습니다.",
    "D": "add_step 함수는 인자로 받은 문자열을 steps 리스트에 추가한 후 그 문자열을 반환하므로, 함수가 호출되는 순서대로 'prompt', 'llm', 'parser'가 차례로 steps에 저장되고 각 변수에 할당됩니다. pipeline 리스트는 add_step의 반환값(문자열)을 직접 저장하는 것이므로 호출 순서 그대로 'prompt | llm | parser'가 출력됩니다. 함수 호출의 순서가 변수 저장 순서를 결정한다는 점을 기억하면 파이프라인의 실행 흐름을 정확히 이해할 수 있습니다."
  },
  "miniboss_adv_os_5_2_009": {
    "B": "invoke 메서드가 호출될 때 전달된 딕셔너리 {'subject': 'AI', 'topic': 'LangChain'}가 **data로 format 메서드의 **kwargs에 언팩되므로, 실제로는 format(subject='AI', topic='LangChain')이 실행되어 {subject}와 {topic}이 각각 'AI'와 'LangChain'으로 치환됩니다. 딕셔너리를 언팩하지 않고 그대로 전달하면 키가 치환되지 않지만, **를 사용한 언팩이 정확히 작동하는 것입니다.",
    "C": "format 메서드가 kwargs.items()를 반복할 때 딕셔너리의 모든 키-값 쌍을 순회하므로, 'subject' 치환 후에 'topic'도 반드시 처리되어 {topic}이 'LangChain'으로 완전히 바뀐다는 뜻입니다. 루프가 한 번만 도는 게 아니라 딕셔너리의 모든 항목에 대해 순차적으로 replace를 수행하므로 모든 플레이스홀더가 값으로 치환된다는 점을 기억하세요.",
    "D": "invoke 메서드가 전달받은 딕셔너리 {'subject': 'AI', 'topic': 'LangChain'}의 키-값 쌍을 그대로 format 메서드의 **kwargs로 넘기므로, {subject}는 'AI'로, {topic}은 'LangChain'으로 각각 치환되는데 선택지 D는 값들이 뒤바뀌어 있습니다. 파이썬의 딕셔너리는 키-값의 대응 관계가 정확하므로, 주어진 키에 맞는 올바른 값으로 치환되어야 합니다."
  },
  "miniboss_adv_os_5_3_002": {
    "B": "메시지를 3번 추가했는데 개수를 2개로 센 것 같네요. 코드를 따라가보면 add_user('안녕'), add_ai('반갑습니다'), add_user('잘 지내?') 순서로 append가 3번 실행되므로 len(history.messages)는 3입니다. 리스트에 요소를 추가할 때마다 길이가 증가한다는 점을 기억하면서 차근차근 코드를 실행해보세요!",
    "C": "세 번의 메서드 호출로 messages 리스트에 3개의 메시지가 추가되는 것은 맞지만, history.messages[1]은 두 번째로 추가된 'AI의 반갑습니다' 메시지이므로 role 값은 'user'가 아니라 'assistant'입니다. 리스트의 인덱싱을 다시 확인해보면, [0]번은 'user'인 '안녕', [1]번은 'assistant'인 '반갑습니다', [2]번은 'user'인 '잘 지내?'이 됩니다.",
    "D": "`add_user`, `add_ai`, `add_user` 총 3번 메시지를 추가했으므로 `len(history.messages)`는 3이지 2가 아니며, `history.messages[1]`은 두 번째 항목인 AI가 추가한 메시지('반갑습니다')를 가리키므로 'role' 값은 'user'가 아니라 'assistant'입니다. 리스트의 인덱스가 0부터 시작한다는 점과 각 메시지가 어느 메서드로 추가되었는지 차례대로 추적하는 것이 중요합니다."
  },
  "miniboss_adv_os_5_3_004": {
    "B": "`get_or_create('user_1')`과 `get_or_create('user_2')`를 각각 호출했으므로 store 딕셔너리에는 'user_1'과 'user_2' 두 개의 키가 생성되어 `len(store)`는 2가 됩니다. `s1_again`은 'user_1'에 저장된 리스트를 참조하고 있으며, 이 리스트에는 이미 'hello'가 추가되었으므로 `len(s1_again)`은 1입니다.\n\n참조(reference) 개념을 이해하면, 같은 키로 반환된 리스트는 같은 객체이며 `append()`로 추가한 원소는 그 객체를 참조하는 모든 변수에서 접근 가능합니다.",
    "C": "`s1_again`은 `get_or_create('user_1')`을 호출할 때 이미 store에 존재하는 'user_1' 키의 리스트를 반환받기 때문에, 앞서 `s1.append('hello')`로 추가된 'hello'가 그대로 들어있어 길이가 1입니다. 파이썬의 리스트는 참조 타입이므로 같은 리스트 객체를 가리키는 모든 변수는 동일한 내용을 공유합니다.",
    "D": "`get_or_create('user_1')`과 `get_or_create('user_2')`를 각각 호출하면 store에 'user_1'과 'user_2' 두 개의 키가 생성되므로 `len(store)`는 1이 아니라 2입니다. 또한 `s1.append('hello')`로 리스트에 'hello'를 추가했으므로 `s1_again`도 같은 리스트를 참조하여 `len(s1_again)`은 0이 아니라 1입니다. 파이썬에서 리스트는 참조 타입이므로, 같은 딕셔너리 값을 가리키는 변수들은 모두 동일한 리스트 객체의 변화를 공유한다는 점을 기억하세요."
  },
  "miniboss_adv_os_5_3_006": {
    "B": "길이는 4가 맞지만, 리스트의 마지막 4개 요소를 가져오는 `messages[-max_messages:]`는 'msg6', 'msg7', 'msg8', 'msg9'를 반환하므로 첫 번째 요소는 'msg0'이 아니라 'msg6'입니다. 파이썬에서 음수 인덱싱은 리스트의 끝에서부터 세어서 지정된 개수만큼 뒤에서 자르는 슬라이싱을 수행합니다.",
    "C": "함수가 max_messages 조건을 확인하지 않고 원본 리스트를 그대로 반환한다고 생각했을 텐데, 실제로는 len(messages)가 10이고 max_messages가 4이므로 조건을 만족하여 messages[-4:]로 마지막 4개 요소만 반환합니다. 리스트 슬라이싱에서 음수 인덱스는 끝에서부터 세어지므로 ['msg6', 'msg7', 'msg8', 'msg9']가 반환되고, 이는 길이 4이며 첫 요소는 'msg6'입니다.",
    "D": "messages[-max_messages:]는 리스트의 마지막 4개 요소를 뒤에서부터 가져오는 것이므로, 실제로는 ['msg6', 'msg7', 'msg8', 'msg9']를 반환하며 trimmed[0]은 'msg6'이 되어 'msg4'가 아닙니다. 음수 인덱싱은 리스트의 끝에서부터 계산하는 방식으로, -max_messages부터 끝까지의 요소를 모두 포함한다는 점을 기억하세요."
  },
  "miniboss_adv_os_5_3_009": {
    "B": "함수가 호출될 때마다 user 메시지와 assistant 메시지가 모두 리스트에 추가되므로, 두 번째 호출 시점에는 store['s1']의 길이가 2가 아니라 3입니다. 첫 번째 호출 후 리스트에 user 메시지(길이 1)와 reply가 추가되어 길이가 2가 되고, 두 번째 호출에서 user 메시지가 또 추가되어 길이 3이 되면서 reply는 '[3] 네!'가 되는 거죠. 함수 내에서 `len(store[session_id])`는 현재까지 쌓인 모든 메시지의 개수를 반영한다는 점을 기억하면 좋겠습니다.",
    "C": "함수가 실행될 때마다 user 메시지를 먼저 append한 후 reply 문자열을 만들기 때문에, 첫 번째 호출에서는 user 메시지 1개만 저장된 상태에서 `len(store['s1'])` = 1이고, 두 번째 호출에서는 user 메시지 1개 + assistant 메시지 1개 + 새로운 user 메시지 1개가 있어서 `len(store['s1'])` = 3이 됩니다. 리스트의 길이를 세는 시점에서 assistant 메시지는 아직 추가되지 않았다는 점을 기억하세요!",
    "D": "함수가 호출될 때마다 store[session_id] 리스트에 user 메시지와 assistant 메시지 두 개가 모두 추가되므로, 첫 번째 호출 후에는 리스트 길이가 2가 되고 두 번째 호출 후에는 4가 되어 `[1] 네!`과 `[1] 네!`이 아니라 `[1] 네!`과 `[3] 네!`이 출력됩니다. 같은 session_id로 여러 번 호출할 때 리스트는 계속 누적되므로, 각 호출에서 reply를 만드는 시점의 리스트 길이는 점점 증가한다는 점을 기억하세요."
  },
  "miniboss_adv_os_5_4_002": {
    "B": "리스트 컴프리헨션에서 조건 `d['metadata']['source'] == 'book'`을 만족하는 문서는 첫 번째 항목 하나뿐이므로 `book_docs`의 길이는 2가 아니라 1입니다. 두 번째와 세 번째 문서의 source는 각각 'web'과 'paper'이기 때문에 필터링 조건을 통과하지 못합니다. 리스트 컴프리헨션은 주어진 조건에 맞는 요소들만 선택하여 새로운 리스트를 만드는 것이므로, 조건을 정확히 확인하며 몇 개의 요소가 실제로 선택되는지 확인해야 합니다.",
    "C": "리스트 컴프리헨션에서 `source == 'book'` 조건을 확인할 때, 두 번째 문서의 source는 'web'이므로 필터링되지 않습니다. 따라서 book_docs에는 '파이썬 기초'만 포함되어 'LangChain 소개'가 아닙니다. 리스트 컴프리헨션은 주어진 조건을 정확히 만족하는 요소들만 선택하는 방식으로 작동합니다.",
    "D": "리스트 컴프리헨션은 조건을 만족하는 원소들을 필터링하므로 오류가 발생하지 않으며, 'source' 값이 'book'인 딕셔너리가 정확히 1개 존재하기 때문에 `book_docs`는 빈 리스트가 아닙니다. 리스트 컴프리헨션과 중첩된 딕셔너리 접근은 올바른 파이썬 문법이므로 조건만 맞으면 정상적으로 실행됩니다."
  },
  "miniboss_adv_os_5_4_004": {
    "B": "chunk_size에서 overlap을 뺀 8 - 2 = 6만큼 start가 증가하기 때문에, start 값이 0 → 6 → 12 → 18 → 24 순으로 변하면서 실제로는 4개의 청크가 생성됩니다. overlap의 효과를 제대로 고려하면 각 반복에서 이전 청크와 겹치는 부분이 생기므로 단순히 chunk_size만으로 나누는 것과는 다른 결과가 나온다는 점을 기억하세요.",
    "C": "청크 크기에만 초점을 맞춰 20을 8로 나누면 5가 나오지만, 실제로는 overlap(겹침)으로 인해 시작 위치가 chunk_size - overlap = 6씩 증가하므로 0 → 6 → 12 → 18에서만 청크가 생성되어 총 4개입니다. 겹침이 있는 슬라이딩 윈도우에서는 시작 위치의 증가폭이 청크 크기보다 작아진다는 점을 기억하세요!",
    "D": "청크 크기가 8이므로 한 번에 2개만 만든다고 생각하셨겠지만, 실제로는 overlap이 2이므로 start가 매번 8-2=6씩 증가하며 0→6→12→18로 움직이기 때문에 4개의 청크가 생성됩니다. 겹침(overlap)이 있을 때는 청크 크기에서 겹침 크기를 빼서 이동 거리를 계산해야 한다는 점을 기억하세요!"
  },
  "miniboss_adv_os_5_4_006": {
    "B": "쿼리 벡터 [1, 0]과의 내적을 계산하면 '파이썬 기초'는 1×1+0×0=1, 'LangChain 소개'는 0×1+1×0=0, '파이썬 심화'는 0.9×1+0.1×0=0.9가 되어 내림차순 정렬 시 [파이썬 기초, 파이썬 심화, LangChain 소개] 순서이므로 ranked[0]은 '파이썬 기초', ranked[-1]은 'LangChain 소개'입니다. 벡터 내적의 크기가 클수록 유사도가 높다고 판단되는 원리를 정확히 이해하면 어떤 문서가 쿼리에 가장 가까운지 쉽게 파악할 수 있습니다.",
    "C": "코드를 직접 실행해보면 각 문서의 내적값이 파이썬 기초 1.0, LangChain 소개 0.0, 파이썬 심화 0.9로 계산되는데, 파이썬 심화(0.9)가 LangChain 소개(0.0)보다 크므로 ranked[-1]은 LangChain 소개가 맞습니다. 내적(dot product)을 이용한 유사도 계산에서 쿼리 벡터와 문서 벡터의 곱셈 합이 클수록 유사하다는 점을 기억하면서 정렬된 리스트의 마지막 요소가 가장 작은 값임을 확인하세요.",
    "D": "파이썬 심화 문서의 벡터 [0.9, 0.1]과 쿼리 벡터 [1, 0]의 내적은 0.9×1 + 0.1×0 = 0.9이고, LangChain 소개의 벡터 [0, 1]과의 내적은 0×1 + 1×0 = 0이므로, 정렬 후 마지막 순위(ranked[-1])는 파이썬 심화가 아니라 LangChain 소개입니다. 벡터의 유사도를 비교할 때는 모든 차원의 곱을 더하는 내적 연산으로 순위가 결정되므로, 각 문서별로 정확히 계산해서 확인해야 합니다."
  },
  "miniboss_adv_os_5_4_009": {
    "B": "sorted() 함수에서 reverse=True를 사용했으므로 점수가 높은 순서대로 정렬되는데, 보기 B는 문서의 원래 순서대로 선택된 것처럼 보이기 때문에 reverse=True 옵션을 놓친 것 같습니다. 실제로는 doc1(0.9), doc5(0.85) 다음으로 doc2(0.7)가 아닌 doc3(0.95)이 세 번째로 높은 점수이므로 올바른 결과는 ['doc3', 'doc1', 'doc5']입니다. sorted()의 reverse=True는 정렬 기준을 내림차순으로 바꾼다는 점을 기억하면 좋습니다!",
    "C": "sorted() 함수에서 reverse=True는 점수를 높은 순서대로 정렬하므로, 실제 순서는 0.95(doc3) → 0.9(doc1) → 0.85(doc5)가 되어 ['doc3', 'doc1', 'doc5']가 출력됩니다. 보기 C는 doc5와 doc1의 순서를 바꾼 것으로, 점수 값의 대소 관계를 제대로 비교하지 않으신 것 같습니다. sorted() 함수의 reverse=True 옵션은 정렬 기준값을 내림차순(큰 것부터 작은 것)으로 배열한다는 점을 기억해 보세요!",
    "D": "점수를 내림차순으로 정렬할 때 가장 높은 점수부터 순서대로 0.95(doc3), 0.9(doc1), 0.85(doc5)이므로, doc5가 세 번째가 아니라 세 번째 요소인 doc5가 맨 뒤에 오게 됩니다. sorted 함수는 주어진 기준에 따라 리스트를 새로운 순서로 정렬하고, [:k]는 정렬된 순서의 앞에서부터 k개를 선택하는 것이라는 점을 기억하세요."
  },
  "miniboss_adv_os_5_5_002": {
    "B": "코드를 실행하면 context는 '파이썬은 범용 언어입니다.\\n\\n'로 시작하지 않고 두 문자열 사이에만 '\\n\\n'이 삽입되므로, split('\\n\\n')의 결과는 ['파이썬은 범용 언어입니다.', 'LangChain은 LLM 프레임워크입니다.']로 길이가 2가 됩니다. split 메서드는 구분자로 나뉜 요소의 개수를 반환하는데, '\\n\\n'이 1개 있으면 2개의 요소로 분리된다는 점을 주의해야 합니다.",
    "C": "두 개의 문자열을 '\\n\\n'으로 연결하면 '파이썬은 범용 언어입니다.\\n\\nLangChain은 LLM 프레임워크입니다.'가 되는데, 이를 '\\n\\n'으로 분할하면 정확히 2개의 요소 리스트만 반환되므로 len() 값은 3이 아닌 2입니다. split()한 결과를 직접 출력해서 확인해보면 ['파이썬은 범용 언어입니다.', 'LangChain은 LLM 프레임워크입니다.'] 리스트의 길이가 2임을 알 수 있을 거예요. 문자열 분할 시 구분자 개수와 결과 요소 개수의 관계(요소 개수 = 구분자 개수 + 1)를 정확히 파악하면 이런 실수를 피할 수 있답니다!",
    "D": "각 문서를 '\\n\\n'(두 줄 띄우기)로 연결하면 \"파이썬은 범용 언어입니다.\\n\\nLangChain은 LLM 프레임워크입니다.\"가 되는데, 이를 '\\n\\n'으로 다시 split하면 정확히 2개의 요소로 나뉘지 4개가 아닙니다. '\\n\\n'(총 4글자)이 구분자가 아니라 '\\n\\n'(한 번의 문자열)이 기준이라는 점이 중요합니다. split() 메서드는 구분자로 문자열을 나누므로, 구분자가 1개 있으면 결과는 2개 항목이 되는 것을 기억하세요."
  },
  "miniboss_adv_os_5_5_004": {
    "B": "retrieve 함수는 쿼리 문자열이 문서의 content에 포함된 것만 필터링하는데, 'python'은 'Python is great'에만 포함되므로 LangChain 문서는 반환되지 않습니다. retrieve 함수는 조건을 만족하는 문서만 리스트로 반환하고, 그 결과물만 generate 함수의 context_docs 인자로 전달되므로 모든 문서가 포함되지 않습니다.",
    "C": "retrieve 함수에서 'python'은 소문자로 변환되어 검색하지만, generate 함수의 f-string에서 question 매개변수는 'What is Python?'이라는 원본 값 그대로 사용되므로 'python'이 아닙니다. f-string의 {question} 위치에는 함수 호출 시 전달된 실제 인자값이 그대로 출력됩니다.",
    "D": "f-string의 포맷 순서를 놓치셨네요. `f'Q: {question} | Context: {context}'`에서 Q가 먼저 오고 Context가 나중에 오기 때문에, 실제 출력은 \"Q: What is Python? | Context: ...\"의 순서가 됩니다. f-string은 문자열 안에서 정의한 순서대로 변수를 배치하므로, 선택지와 다른 결과가 나오게 돼요!"
  },
  "miniboss_adv_os_5_5_006": {
    "B": "코드를 단계별로 따라가면 sorted()에서 'python'이 포함된 문서들이 True(1)로 평가되어 앞에 정렬되므로, ranked[:top_k]는 'Python basics'와 'Python advanced' 두 개의 문서를 선택하게 됩니다. 이후 리스트 컴프리헨션에서 두 문서 모두 조건을 만족하므로 context의 길이는 2가 되는데, 1을 고르셨다면 필터링 단계에서 일부 문서만 제외된다고 생각하신 것 같습니다. sorted()의 정렬 결과와 리스트 컴프리헨션의 필터링이 같은 조건을 사용할 때는 상관없지만, 두 단계를 명확히 구분해서 각각 어떤 결과를 반환하는지 확인하는 습관을 들이시면 좋습니다.",
    "C": "sorted() 함수가 쿼리를 포함하는 문서를 우선순위로 정렬하지만, top_k=2 때문에 상위 2개 문서만 선택되므로, 'Python basics'와 'Python advanced' 2개 문서만 context_docs에 포함됩니다. 따라서 context 리스트의 길이는 3이 아니라 2이며, 이는 top_k 파라미터로 처리되는 문서 개수를 제한하는 것이 RAG 파이프라인의 핵심입니다.",
    "D": "쿼리 'python'이 문서의 텍스트에 포함되는지 확인할 때 `query.lower() in d['text'].lower()`를 사용하므로 'python'은 'Python basics'와 'Python advanced' 모두에 포함되며, 이 두 문서가 `ranked[:top_k]`로 선택됩니다. 리스트 컴프리헨션에서 두 문서 모두 조건을 만족하므로 `len(context)`는 2가 되는데, 0을 선택했다면 검색 결과가 없다고 잘못 생각한 것 같습니다. 문자열의 `in` 연산자는 대소문자를 구분하므로 `.lower()`로 통일하면 대소문자 관계없이 포함 여부를 판단할 수 있습니다."
  },
  "miniboss_adv_os_5_5_009": {
    "B": "첫 번째 호출 `chat_rag('Q1', [docs[0]], history)`에서 `retrieved_docs`는 `[docs[0]]`이므로 `{'text': 'Python'}` 하나만 포함되어 context는 'Python'이 되는데, 보기 B는 'Python LangChain'으로 잘못 읽었습니다. 두 번째 호출에서 `len(history)`는 2가 되어 `[turn 2]`가 출력되므로, 각 호출에서 전달된 retrieved_docs의 개수를 정확히 확인하는 것이 중요합니다.",
    "C": "코드를 순차적으로 실행하면 첫 번째 chat_rag 호출에서 history에 첫 번째 턴이 추가되어 len(history)는 1이 되고, 두 번째 호출에서 두 번째 턴이 추가되어 len(history)는 2가 되므로 출력 순서는 [turn 1], [turn 2]가 맞습니다. print 함수는 함수가 호출된 순서대로 결과를 출력하므로 r1을 먼저 출력한 후 r2를 출력하게 되는데, 이는 단순히 리스트를 역순으로 정렬하거나 turn 번호를 임의로 바꾸지 않습니다. 함수 실행 순서와 리스트의 append 동작을 이해하면 각 턴의 번호와 출력 순서가 코드의 실행 흐름과 일치함을 알 수 있습니다.",
    "D": "이 선택지는 history 리스트가 누적되지 않는다고 잘못 생각한 것 같습니다. 첫 번째 호출에서 history에 turn을 append하면 len(history)가 1이 되고, 두 번째 호출에서는 이미 길이가 1이므로 len(history)는 2가 되어 '[turn 2]'가 출력됩니다. 파이썬에서 리스트는 mutable 객체이므로 함수 내에서 append한 데이터가 실제로 원본 history 객체에 누적된다는 점을 기억해두세요."
  },
  "q2502": {
    "B": "HumanMessage는 메시지 객체의 클래스 이름일 뿐, msg 변수에 저장된 객체 자체를 출력하는 것이 아니라 객체의 content 속성값을 출력하고 있습니다. 객체의 특정 속성에 접근하려면 점 표기법(.)을 사용해 속성명을 명시해야 하며, 여기서 msg.content는 객체 생성 시 전달한 \"LangChain 배우기\" 문자열을 반환합니다.",
    "C": "HumanMessage 객체의 type 속성을 출력하면 'human'이 나오지만, 이 코드는 content 속성을 출력하고 있으므로 메시지에 저장된 실제 텍스트인 \"LangChain 배우기\"가 출력됩니다. 속성명을 명확히 구분하면 msg.content는 메시지의 내용을, msg.type은 메시지의 종류를 나타낸다는 것을 알 수 있습니다.",
    "D": "HumanMessage 객체를 생성할 때 content 매개변수에 \"LangChain 배우기\"라는 문자열을 전달했으므로, msg.content는 그 문자열 값 자체를 가지고 있으며 절대 None이 아닙니다. .content 속성은 메시지의 실제 내용을 저장하는 속성이기 때문에, 객체 생성 시 명시적으로 전달된 값이 그대로 반환됩니다."
  },
  "q2504": {
    "A": "PromptTemplate의 `format()` 메서드는 템플릿의 중괄호 안의 변수를 실제 값으로 치환하는 역할을 하는데, 여기서는 `{name}`이 전달받은 \"에이몬\"으로 바뀌지 않고 그대로 출력된다고 생각하신 것 같습니다. 실제로 `template.format(name=\"에이몬\")`을 실행하면 `{name}` 자리에 \"에이몬\" 문자열이 대입되어 \"에이몬님, 환영합니다!\"가 출력됩니다. PromptTemplate의 `format()` 메서드는 전달된 키워드 인자의 이름과 템플릿 내 중괄호 변수를 매칭하여 값을 치환하는 기능입니다.",
    "C": "`format(name=\"에이몬\")` 메서드가 템플릿의 `{name}` 플레이스홀더를 실제 값인 \"에이몬\"으로 치환하기 때문에, \"name\"이라는 문자열이 그대로 출력되지 않습니다. PromptTemplate의 `format()` 메서드는 중괄호로 감싼 변수명을 해당 인자값으로 동적으로 교체하는 기능입니다.",
    "D": "PromptTemplate.from_template()는 문자열 템플릿을 받아서 중괄호 안의 변수를 자동으로 인식하고, format() 메서드로 해당 변수에 값을 대입하는 정상적인 기능이므로 이 코드는 오류 없이 정상 실행됩니다. PromptTemplate는 LangChain에서 제공하는 표준 기능으로, {name} 같은 플레이스홀더를 처리하도록 설계되어 있으니 자신있게 템플릿 기반 문자열 생성을 활용해 보세요!"
  },
  "q2506": {
    "A": "prompt.format_messages()는 메시지 객체 리스트를 반환하는데, msgs[0]은 첫 번째 메시지인 system 역할의 메시지 객체이고, 그 content 속성에는 \"Hello를 번역해줘\"가 아니라 system 프롬프트인 \"당신은 번역기입니다.\"가 저장되어 있습니다. ChatPromptTemplate에서 from_messages()로 정의한 메시지들은 순서대로 배열에 저장되므로, 메시지 리스트의 인덱스와 각 메시지의 역할을 구분하는 것이 중요합니다.",
    "B": "`format_messages()`는 메시지 객체의 리스트를 반환하는데, `msgs[0]`은 첫 번째 메시지 객체이고 `.content` 속성은 그 메시지의 내용 텍스트를 가져오는 것이지, 메시지의 역할(role)을 가져오는 것이 아닙니다. 메시지 객체에서 역할을 알고 싶다면 `.type` 속성을 사용해야 한다는 점을 기억하세요.",
    "D": "`format_messages()`는 메시지 객체들의 리스트를 반환하는데, `msgs[0]`은 리스트의 첫 번째 요소인 system 메시지 객체이고, `.content` 속성은 그 메시지의 실제 내용인 \"당신은 번역기입니다.\"를 가져오는 거라서 역할 이름인 \"system\"이 아니라 메시지의 내용이 출력됩니다. 메시지 객체는 역할(role)과 내용(content)을 분리해서 저장하기 때문에, `.content` 속성을 사용하면 항상 메시지의 텍스트 내용만 접근할 수 있습니다."
  },
  "q2508": {
    "A": "AIMessage는 메시지 객체의 클래스 이름이지만, `type` 속성은 메시지의 종류를 문자열로 나타내는 별도의 속성으로 객체 자체가 아닌 \"ai\"라는 문자열 값을 반환합니다. 클래스 이름과 메시지 타입 속성값을 구분하는 것이 중요하니까요!",
    "B": "LangChain의 AIMessage 클래스에서 `type` 속성은 메시지의 역할을 나타내는데, 이것은 OpenAI의 role 개념과 다르게 'ai'라는 고유한 식별자를 반환합니다. 'assistant'는 OpenAI API에서 사용되는 용어일 뿐, LangChain의 내부 메시지 타입으로는 'ai'가 표준으로 정의되어 있습니다. LangChain의 메시지 타입은 프레임워크의 자체 규칙을 따르므로 외부 API의 명칭과는 구분해서 이해하는 것이 중요합니다.",
    "D": "`msg.type`은 메시지 객체의 타입을 나타내는 속성으로, 해당 메시지의 클래스 이름이 아니라 메시지 역할을 구분하는 문자열 값인 \"ai\"를 반환합니다. 만약 메시지 객체 자체의 타입을 확인하려면 `type(msg)`를 사용해야 하는데, 이 경우에만 `<class 'langchain_core.messages.ai.AIMessage'>`가 출력됩니다. LangChain의 메시지 객체에서 `.type` 속성은 메시지의 역할을 식별하는 고정된 문자열이며, AIMessage는 항상 \"ai\"를 반환한다는 점을 기억하세요!"
  },
  "q2510": {
    "A": "템플릿 문자열에서 중괄호로 감싸진 변수는 {name}, {subject}, {score} 총 3개이므로, input_variables 리스트의 길이는 1이 아니라 3입니다. PromptTemplate의 input_variables는 템플릿에 포함된 모든 변수를 자동으로 추출하여 리스트로 저장하는 속성입니다.",
    "B": "템플릿 문자열에서 중괄호로 감싸진 변수가 {name}, {subject}, {score} 3개이므로 input_variables의 길이는 3인데, 혹시 {name}과 {subject}만 세어서 2개라고 생각하셨나요? PromptTemplate은 템플릿 문자열에 포함된 모든 플레이스홀더를 자동으로 인식하여 input_variables 리스트에 추가합니다.",
    "D": "템플릿 문자열에서 중괄호로 감싼 변수는 {name}, {subject}, {score} 총 3개이므로, prompt.input_variables의 길이는 4가 아니라 3입니다. input_variables는 템플릿에 있는 모든 플레이스홀더를 정확히 세어야 하며, 같은 변수가 여러 번 나타나도 한 번만 카운트됩니다."
  },
  "q2512": {
    "B": "HumanMessage 객체 자체의 이름과 해당 객체의 type 속성은 다른 것입니다. msg.type은 메시지의 종류를 문자열로 나타내는 속성이므로 클래스 이름인 \"HumanMessage\"가 아니라 \"human\"이 출력됩니다. Python에서 객체의 속성(attribute)에 접근할 때는 객체가 무엇인지가 아니라 그 속성이 저장하고 있는 실제 값을 반환한다는 점을 기억하세요.",
    "C": "LangChain의 HumanMessage 객체에서 type 속성은 메시지의 역할을 나타내는 식별자로, \"user\"가 아닌 \"human\"이라는 문자열을 반환합니다. \"user\"는 일반적인 채팅 API에서 사용되는 용어이지만, LangChain에서는 메시지 타입을 \"human\", \"ai\", \"system\" 등으로 구분하고 있어요. LangChain의 메시지 객체는 타입별로 정해진 문자열 값을 가지므로, 실제 코드를 실행하면 \"human\"이 출력됩니다.",
    "D": "msg.type은 메시지 객체의 타입 속성(attribute)을 출력하는 것이지, 변수 msg 자체의 데이터 타입을 확인하는 type() 함수와는 다릅니다. LangChain의 HumanMessage 클래스는 내부적으로 type 속성을 \"human\"이라는 문자열 값으로 정의하고 있습니다. 메시지 객체의 특정 속성값을 조회할 때는 그 속성이 저장하고 있는 실제 값을 반환하며, HumanMessage의 type 속성은 메시지 종류를 나타내는 문자열입니다."
  },
  "q2514": {
    "A": "format_messages() 메서드는 ChatPromptTemplate에 정의된 모든 메시지 튜플을 각각 별개의 메시지 객체로 변환하므로, (\"system\", \"당신은 AI입니다.\")와 (\"human\", \"질문: {q}\")는 총 2개의 메시지가 되어 len(msgs)는 2입니다. ChatPromptTemplate은 여러 개의 메시지 역할(system, human, assistant 등)을 조합하여 하나의 메시지 시퀀스를 만드는 것이 특징입니다.",
    "C": "ChatPromptTemplate.from_messages()에 전달된 튜플은 (역할, 내용) 형태이며, 이 경우 \"system\"과 \"human\" 두 개의 메시지만 생성되므로 len(msgs)는 3이 아닌 2입니다. format_messages() 메서드는 템플릿의 변수({q})를 값으로 채우기만 할 뿐 메시지의 개수를 늘리지 않습니다.",
    "D": "ChatPromptTemplate.from_messages()는 튜플로 제공된 (\"system\", ...), (\"human\", ...) 두 개의 메시지 쌍을 모두 유효한 메시지로 인식하여 format_messages()를 통해 정상적으로 처리되므로 오류가 발생하지 않습니다. ChatPromptTemplate는 system과 human 역할의 메시지를 모두 지원하며, format_messages() 메서드는 템플릿 변수를 치환한 후 메시지 객체 리스트를 반환합니다."
  },
  "q2516": {
    "A": "PromptTemplate의 format() 메서드는 템플릿 문자열의 중괄호로 감싼 변수들을 실제 값으로 치환하는데, 여기서는 {lang}이 \"영어\"로, {word}가 \"안녕\"으로 모두 대체되어야 하므로 중괄호가 그대로 남아있으면 안 됩니다. PromptTemplate.from_template()은 문자열 포맷팅을 수행하는 것이 아니라 템플릿 객체를 생성하는 것이고, format()을 호출할 때 비로소 변수 치환이 일어나는 거예요.",
    "C": "PromptTemplate의 format() 메서드는 전달된 모든 변수를 템플릿에 치환하는데, 여기서는 lang=\"영어\"와 word=\"안녕\" 두 개의 인자가 모두 제공되었으므로 {lang}과 {word} 모두 치환됩니다. 선택지 C처럼 {word}가 그대로 남아있는 것은 format() 호출 시 word 인자를 빠뜨렸을 때의 결과입니다. PromptTemplate에서 format()은 제공된 모든 변수 값으로 중괄호 부분을 바꾸는 메서드라는 점을 기억하세요!",
    "D": "LangChain의 PromptTemplate은 from_template() 메서드로 템플릿 문자열을 파싱하고 format() 메서드로 변수를 치환하도록 설계되었기 때문에, 올바른 라이브러리 설치 환경에서는 이 코드가 정상 작동하여 오류가 발생하지 않습니다. PromptTemplate.format()은 중괄호로 감싼 변수명들을 실제 값으로 단순 치환하는 기능이므로, 전달된 인자가 모두 문자열일 때는 예외 없이 완성된 프롬프트 문자열을 반환합니다."
  },
  "q2518": {
    "A": "msgs 리스트에서 인덱스 0번 위치는 system 메시지이고 인덱스 1번 위치는 human 메시지이기 때문에, msgs[1].content는 system 역할의 \"당신은 번역기입니다.\"가 아니라 human 역할의 포맷팅된 메시지 내용인 \"Hello을 번역해줘\"를 출력합니다. ChatPromptTemplate에서 format_messages()로 생성된 메시지 리스트는 from_messages()에 정의한 순서대로 배열되므로, 올바른 인덱스 위치를 파악하는 것이 중요합니다.",
    "C": "주어진 코드에서 `format_messages(input=\"Hello\")`는 템플릿의 `{input}` 부분을 \"Hello\"로 치환하므로, `msgs[1]`은 \"Hello을 번역해줘\"라는 완전한 내용을 가진 human 메시지입니다. C번 선택지는 템플릿 변수인 `{input}`의 값만 추출한 것으로, 실제 메시지의 전체 내용인 \"Hello을 번역해줘\"를 놓쳤네요. `format_messages()`는 템플릿의 모든 자리에 값을 채워서 완성된 메시지 전체를 반환한다는 점을 기억하세요!",
    "D": "`msgs[1]`은 리스트의 두 번째 요소인 human 역할의 메시지 객체를 가리키는데, `.content` 속성을 붙이면 그 메시지 객체의 내용 문자열을 반환하므로 \"human\"이라는 역할 이름이 아니라 실제 메시지 내용인 \"Hello을 번역해줘\"가 출력됩니다. ChatPromptTemplate의 메시지 객체는 역할 정보와 실제 내용을 분리하여 저장하고 있으며, `.content`를 통해 내용에만 접근할 수 있습니다."
  },
  "q2520": {
    "A": "프롬프트 템플릿에서 중괄호로 감싸진 변수는 {role}, {question}, {language} 총 3개이지만, A번 답안은 role과 question만 세어서 2개로 계산한 것 같습니다. input_variables는 템플릿 문자열 전체에서 찾은 모든 변수를 포함하므로 system 메시지의 {role}도 함께 집계됩니다. ChatPromptTemplate의 input_variables는 모든 메시지에 포함된 플레이스홀더를 수집하므로, 메시지의 역할(system, human)에 관계없이 모든 변수를 통합적으로 인식합니다.",
    "C": "프롬프트 템플릿의 input_variables는 중괄호 안의 변수들을 자동으로 추출하는데, {role}, {question}, {language} 이렇게 3개의 서로 다른 변수만 포함되므로 4가 아닙니다. 중괄호로 감싼 모든 문자를 센 것이 아니라 중복을 제거한 고유한 변수의 개수를 세어야 함을 기억하세요.",
    "D": "ChatPromptTemplate.from_messages()는 문자열 템플릿에서 중괄호로 감싼 변수들을 자동으로 인식하기 때문에, 코드에 문법 오류가 없다면 정상 실행되며 이 경우 {role}, {question}, {language} 3개의 입력 변수가 추출되어 오류 없이 3을 출력합니다. LangChain의 프롬프트 템플릿은 변수 개수와 관계없이 유연하게 처리되도록 설계되어 있습니다."
  },
  "q2522": {
    "B": "StrOutputParser는 AIMessage 객체를 받아서 그 content 속성인 \"에이몬\"이라는 문자열을 추출하여 반환하기 때문에, result는 문자열 타입이 되어 isinstance(result, str)은 True를 출력합니다. 파서의 역할은 LLM의 응답 메시지 객체에서 실제 텍스트 내용을 뽑아내는 것임을 기억하세요!",
    "C": "StrOutputParser의 invoke 메서드는 AIMessage 객체를 받아서 content 속성인 \"에이몬\" 문자열을 추출해 반환하므로, result는 None이 아니라 str 타입의 \"에이몬\"이 됩니다. LangChain의 출력 파서는 메시지 객체에서 실제 텍스트 내용을 추출하여 문자열로 변환하는 역할을 한다는 점을 기억하세요!",
    "D": "StrOutputParser는 AIMessage 객체를 받아서 내용을 자동으로 추출하는 parser이므로 오류가 아니라 정상적으로 \"에이몬\"이라는 문자열을 반환합니다. parser.invoke()가 AIMessage의 content 속성을 문자열로 변환하기 때문에 isinstance(result, str)은 True를 출력하게 됩니다."
  },
  "q2524": {
    "B": "StrOutputParser는 입력받은 객체의 내용물을 추출하여 문자열로 변환하는 파서이기 때문에, AIMessage 객체 자체를 그대로 반환하는 것이 아니라 그 안의 content 값인 \"LangChain은 LLM 앱 프레임워크입니다.\"만 추출해서 출력합니다. 파서의 역할은 복잡한 객체 구조에서 필요한 데이터만 깔끔하게 뽑아내는 것이라는 점을 기억해두세요!",
    "C": "AIMessage 객체는 딕셔너리가 아니라 메시지 객체이기 때문에 StrOutputParser가 invoke()할 때 객체 전체를 딕셔너리로 변환하지 않고, 메시지의 content 속성 값만 문자열로 추출합니다. StrOutputParser는 입력받은 메시지나 텍스트에서 순수한 문자열 내용만을 뽑아내는 파서이므로, 데이터 구조나 타입 정보는 제거되고 실제 텍스트 값만 반환됩니다.",
    "D": "StrOutputParser의 invoke() 메서드는 입력받은 AIMessage 객체에서 content 속성을 추출하여 문자열로 반환하므로, 실제 출력값은 \"LangChain은 LLM 앱 프레임워크입니다.\"라는 문자열이지 None이 아닙니다. LangChain의 출력 파서는 메시지 객체를 처리하여 실제 텍스트 내용을 반환하는 역할을 합니다."
  },
  "q2526": {
    "A": "변수 `role`에 저장된 값은 \"Python\"이지만, `msgs[0]`은 시스템 메시지 객체의 첫 번째 요소이고 `.content` 속성은 메시지의 전체 내용 문자열을 반환하므로 \"Python\"만 출력되지 않습니다. `format_messages()` 메서드는 템플릿의 플레이스홀더를 모두 치환하여 완성된 문자열 \"당신은 Python 전문가입니다.\"를 content로 만듭니다. 템플릿 문자열에서 플레이스홀더는 단순한 변수명이 아니라 전체 메시지 문맥 속에서 치환되는 부분이라는 점을 기억하세요.",
    "B": "format_messages() 메서드는 템플릿의 {role}과 {question} 같은 플레이스홀더를 실제 값으로 치환하는데, 보기 B는 치환이 일어나지 않은 원본 템플릿 문자열을 선택한 것입니다. msgs[0]은 system 메시지 객체이고 .content 속성으로 접근하면 이미 \"Python\"이 {role} 자리에 대입된 \"당신은 Python 전문가입니다.\"가 출력됩니다. ChatPromptTemplate의 format_messages()는 변수를 실제 값으로 치환하여 Message 객체를 생성하므로, 플레이스홀더가 남아있는 상태는 발생하지 않습니다.",
    "D": "이 코드에서 msgs[0]은 prompt의 첫 번째 메시지인 system 메시지를 의미하는데, D번은 두 번째 메시지인 human 메시지의 내용이기 때문입니다. format_messages()로 포맷팅된 메시지 리스트에서 인덱스 0번은 항상 가장 먼저 정의된 시스템 메시지를 가리킵니다. ChatPromptTemplate의 메시지 순서는 from_messages()에서 정의한 순서대로 저장되므로, 필요한 메시지의 위치를 정확히 파악하는 것이 중요합니다."
  },
  "q2528": {
    "A": "리스트의 인덱스를 헷갈리신 것 같습니다. `results[1]`은 리스트의 두 번째 원소를 의미하므로(0번 인덱스부터 시작), `parser.invoke(AIMessage(content=\"첫 번째\"))`의 결과가 아니라 `parser.invoke(AIMessage(content=\"두 번째\"))`의 결과인 \"두 번째\"를 출력합니다. 파이썬에서 리스트는 0부터 시작하는 인덱싱을 사용하므로, `results[0]`이 첫 번째 원소, `results[1]`이 두 번째 원소입니다.",
    "C": "리스트 인덱싱에서 `results[1]`은 리스트의 두 번째 요소를 의미하는데, 코드에서 생성된 리스트는 `['첫 번째', '두 번째', '세 번째']`이므로 인덱스 1은 '두 번째'입니다. 세 번째 요소에 접근하려면 `results[2]`를 사용해야 합니다. 파이썬에서는 리스트의 인덱싱이 0부터 시작하므로, 원하는 위치의 요소를 정확히 파악하는 것이 중요합니다.",
    "D": "StrOutputParser는 각 메시지의 content를 개별적으로 추출하여 리스트 컴프리헨션을 통해 ['첫 번째', '두 번째', '세 번째'] 형태의 리스트를 만들지만, print(results[1])은 이 전체 리스트가 아니라 리스트의 인덱스 1번 요소만 출력하므로 '두 번째'가 맞습니다. 리스트 컴프리헨션의 결과 전체가 아니라 해당 인덱스의 요소값 하나를 선택해야 한다는 점을 기억하세요."
  },
  "q2530": {
    "B": "StrOutputParser는 입력값이 문자열이 아닐 때 문자열로 변환하지만, AIMessage의 content 속성이 빈 문자열(\"\")이므로 그대로 빈 문자열을 반환합니다. None을 반환하는 것이 아니라 실제로 content 속성의 값 자체(\"\")가 출력되는 것이죠. LangChain의 StrOutputParser는 메시지 객체에서 텍스트 내용을 추출할 때 내용이 없으면 빈 문자열을 유지하며, 절대 None으로 변환하지 않습니다.",
    "C": "`StrOutputParser`는 입력값을 문자열로 변환하는 파서인데, 빈 문자열 `\"\"`도 문자열이므로 정수 0이 아니라 그대로 빈 문자열 `''`로 반환됩니다. 숫자와 문자열은 완전히 다른 자료형이기 때문에 헷갈리기 쉽지만, 파이썬에서 빈 문자열은 절대 0으로 변환되지 않습니다. 파서의 역할은 입력을 문자열 타입으로 유지하거나 변환하는 것이지, 문자열의 길이나 값을 숫자로 바꾸는 것이 아님을 기억하세요.",
    "D": "StrOutputParser는 AIMessage 객체를 받으면 content 속성에서 문자열을 추출하기만 하므로, content가 빈 문자열(\"\")이어도 정상적으로 작동하여 오류가 발생하지 않습니다. 빈 문자열도 유효한 문자열 값이므로 parser.invoke()는 \"\"를 반환하며, 이는 repr()로 감싸도 ''로 출력될 뿐입니다."
  },
  "q2532": {
    "A": "StrOutputParser는 AIMessage 객체에서 content 속성의 문자열 값을 추출하므로, \"Hello, LangChain!\" 전체 문자열이 반환되며 이 문자열의 길이는 1이 아닌 17입니다. len() 함수는 문자열의 총 문자 개수를 세기 때문에 공백과 쉼표, 느낌표를 모두 포함하여 계산됩니다.",
    "C": "StrOutputParser는 AIMessage 객체에서 content 속성의 문자열 값을 추출하므로, \"Hello, LangChain!\" 전체 문자열이 반환되며 이의 길이는 2가 아닌 17입니다. 문자열의 길이는 공백과 특수문자를 포함하여 모든 문자를 세므로, \"Hello, LangChain!\"의 각 문자를 하나씩 세면 정확히 17개가 됩니다.",
    "D": "StrOutputParser는 AIMessage 객체에서 content 속성의 문자열 값을 추출하므로, \"Hello, LangChain!\"이라는 17개의 문자(공백과 쉼표 포함)가 반환되어 len(result)는 0이 아닌 17입니다. 파서가 메시지 객체 자체를 무시하고 내용물을 꺼내는 역할을 한다는 점을 기억하면, 빈 결과가 아니라 실제 문자열이 반환된다는 것을 알 수 있어요!"
  },
  "q2534": {
    "A": "ChatPromptTemplate.from_messages()는 전달한 튜플 리스트의 각 항목을 하나의 메시지 객체로 변환하므로, (\"system\", \"당신은 AI입니다.\"), (\"human\", \"{question}\"), (\"ai\", \"물론이죠!\") 세 개의 메시지가 생성되어 len(msgs)의 값은 3입니다. format_messages() 메서드는 변수를 치환하기만 할 뿐 메시지의 개수를 줄이지 않으므로, 한 개의 메시지만 있다고 생각하신 것 같습니다. 프롬프트 템플릿에서 정의한 모든 역할의 메시지는 각각 독립적인 객체로 유지되며, 그 개수를 정확히 파악하는 것이 중요합니다.",
    "B": "ChatPromptTemplate.from_messages()에 전달된 튜플이 (\"system\", \"당신은 AI입니다.\"), (\"human\", \"{question}\"), (\"ai\", \"물론이죠!\") 세 개이기 때문에 format_messages()는 이 세 개를 모두 메시지 객체로 변환하여 길이 3인 리스트를 반환합니다. ChatPromptTemplate은 입력 변수({question})만 세는 것이 아니라 정의된 모든 메시지 역할(system, human, ai)을 메시지로 포함시킵니다.",
    "D": "ChatPromptTemplate.from_messages()는 시스템 메시지, 인간 메시지, AI 메시지 등 여러 역할의 메시지를 등록할 수 있도록 설계되었으며, 이 코드는 문법적으로 올바르게 작성되어 오류 없이 정상 실행됩니다. ChatPromptTemplate에서 format_messages() 메서드를 호출하면 등록된 모든 메시지 템플릿이 처리되어 리스트 형태로 반환되므로, system, human, ai 3개 메시지가 모두 포함됩니다."
  },
  "q2536": {
    "A": "msgs[1]은 prompt의 두 번째 메시지인 human 역할의 메시지를 참조하는데, 첫 번째 메시지(system)인 \"당신은 요약기입니다.\"가 아니라 format_messages()로 포맷팅된 human 메시지의 내용이 들어가 있습니다. ChatPromptTemplate의 메시지 리스트에서 인덱스 0은 system 메시지, 인덱스 1은 human 메시지이므로 msgs[1]의 content에는 변수가 치환된 \"긴 문서를 3줄로 요약해줘\"가 출력됩니다.",
    "B": "format_messages() 메서드는 템플릿의 {text}와 {lines} 같은 변수를 실제 값으로 치환하기 때문에, 보기 B처럼 원본 템플릿 문자열이 그대로 출력되지 않습니다. msgs[1]은 \"human\" 역할의 메시지이며 실제로는 \"긴 문서를 3줄로 요약해줘\"로 변환되어 있죠. ChatPromptTemplate에서 format_messages()를 호출하면 모든 변수가 전달된 인자값으로 자동 치환되므로, 템플릿 구조를 이해하고 어떤 메시지가 어떻게 처리되는지 확인해야 합니다.",
    "D": "format_messages() 메서드는 템플릿의 모든 플레이스홀더를 전달된 인자값으로 치환하므로, {text}는 \"긴 문서\"로, {lines}는 \"3\"으로 모두 변환되어 긴 문서를 3줄로 요약해줘가 됩니다. ChatPromptTemplate에서 format_messages()는 부분 치환이 아닌 완전한 치환을 수행하니까 이 점을 기억해두세요!"
  },
  "q2538": {
    "A": "StrOutputParser는 입력받은 메시지 객체 자체를 그대로 반환하지 않고, 메시지의 content 속성에서 문자열만 추출하여 반환하기 때문에 AIMessage 객체가 남아있지 않습니다. StrOutputParser의 역할은 LLM의 출력을 문자열로 파싱하는 것이므로, invoke 메서드를 호출하면 'content' 값에 해당하는 문자열만 추출됩니다.",
    "C": "StrOutputParser는 AIMessage 객체를 받아서 content 속성값만 추출하므로, 리스트 컴프리헨션으로 range(3)을 반복하면 '결과0', '결과1', '결과2' 세 개의 문자열이 모두 생성됩니다. 반복문이 3번 실행되어 총 3개의 원소가 리스트에 담기므로 ['결과0']처럼 첫 번째 결과만 나오지는 않습니다.",
    "D": "StrOutputParser는 AIMessage 객체를 받아서 content 속성만 추출하는 파서이므로, 코드는 정상적으로 실행되며 오류가 발생하지 않습니다. LangChain의 output_parser들은 다양한 메시지 타입을 처리하도록 설계되었으니, 어떤 파서가 어떤 입력을 받을 때 동작하는지 확인하며 학습하면 좋겠어요!"
  },
  "q2540": {
    "A": "이 코드에서 중괄호로 감싸진 변수는 {role}, {question}, {followup} 총 3개이며, \"ai\" 역할의 메시지는 고정된 문자열이라 변수를 포함하지 않으므로 입력 변수 개수에 포함되지 않습니다. ChatPromptTemplate의 input_variables는 템플릿에서 실제로 치환되어야 하는 모든 변수를 추적하기 때문에 len(prompt.input_variables)는 2가 아닌 3을 반환합니다.",
    "C": "프롬프트 템플릿에서 입력 변수는 중괄호로 표시된 변수 중 실제로 사용자가 제공해야 하는 것들만 포함되는데, \"ai\" 역할의 메시지는 고정된 응답이므로 {role}, {question}, {followup} 3개만 입력 변수로 인식됩니다. \"ai\" 메시지처럼 변수 없이 고정된 콘텐츠는 입력 변수 계산에 포함되지 않으므로, 템플릿에 4개의 메시지가 있다고 해서 입력 변수가 4개인 것은 아닙니다.",
    "D": "ChatPromptTemplate.from_messages()는 주어진 메시지 리스트에서 중괄호로 감싸진 변수를 자동으로 인식하여 input_variables를 생성하므로 오류 없이 정상 실행되며, 실제로 {role}, {question}, {followup} 세 개의 고유한 변수가 감지됩니다. LangChain의 프롬프트 템플릿은 유효한 변수 형식이라면 메시지 구조와 관계없이 모두 처리할 수 있는 유연한 설계를 가지고 있습니다."
  },
  "q2542": {
    "B": "InMemoryChatMessageHistory 객체를 생성할 때 아무런 메시지도 추가하지 않았기 때문에 messages 리스트는 초기에 비어있으며, 따라서 len()의 결과는 0입니다. 채팅 기록은 add_message() 같은 메서드로 명시적으로 메시지를 추가해야만 리스트에 저장되기 시작합니다.",
    "C": "`len()` 함수는 리스트나 컬렉션의 길이를 반환하는데, `history.messages`는 초기화 직후 비어있는 리스트이므로 `None`이 아니라 정수값 0을 반환합니다. `len()`은 항상 정수를 반환하며, `None`을 반환하는 경우는 명시적으로 `None`을 리턴하는 함수에서만 발생합니다.",
    "D": "InMemoryChatMessageHistory는 LangChain에서 제공하는 표준 클래스로, 초기화 시점에 messages 속성이 빈 리스트로 자동 설정되어 정상적으로 작동하므로 오류가 발생하지 않습니다. 객체 생성 후 즉시 len()으로 messages의 길이를 구하면 아무 메시지도 추가되지 않았으므로 0이 출력됩니다."
  },
  "q2544": {
    "B": "코드에서 `add_user_message()`와 `add_ai_message()`를 각각 한 번씩 호출했으므로 메시지가 2개 저장되는데, 1개만 저장된다고 생각하신 것 같습니다. 각 메서드는 독립적으로 하나의 메시지를 히스토리에 추가하므로 총 2개의 메시지가 쌓이게 됩니다. `history.messages` 리스트에는 추가된 모든 메시지가 순서대로 저장되어 있으니, 메서드 호출 횟수만큼 길이가 증가한다고 기억하면 좋습니다.",
    "C": "사용자 메시지 1개와 AI 메시지 1개를 각각 `add_user_message()`와 `add_ai_message()`로 추가했으므로 `history.messages`에는 총 2개의 메시지 객체만 저장되는데, 4를 선택하신 것은 메시지 문자열의 길이나 단어 수를 세신 것 같습니다. `len()`은 리스트에 포함된 메시지 객체의 개수를 세기 때문에 결과는 2입니다.",
    "D": "이 코드에서 `add_user_message()`와 `add_ai_message()` 메서드를 각각 한 번씩 호출했으므로 history 객체에 메시지가 2개 저장되어 `len(history.messages)`는 0이 아니라 2를 반환합니다. InMemoryChatMessageHistory는 메서드 호출 시점에 메시지를 즉시 메모리에 저장하므로 print 문이 실행될 때 이미 두 메시지가 저장된 상태입니다."
  },
  "q2546": {
    "B": "`add_user_message()` 메서드로 추가된 메시지는 사용자가 입력한 메시지이므로 type 속성이 \"ai\"가 아니라 \"human\"으로 저장됩니다. LangChain에서는 사용자 메시지의 type을 \"human\"으로, AI의 응답을 \"ai\"로 구분하기 때문에 누가 메시지를 보냈는지 정확히 파악하는 것이 중요합니다.",
    "C": "system은 시스템 메시지의 타입이지만, 코드에서는 add_user_message() 메서드로 사용자 메시지를 추가했으므로 메시지 타입은 \"system\"이 아닙니다. LangChain에서 사용자가 보낸 메시지는 내부적으로 \"human\" 타입으로 저장되므로, 실제 출력 결과는 \"human\"이 됩니다.",
    "D": "`add_user_message()` 메서드로 추가된 메시지의 타입은 \"user\"가 아니라 \"human\"으로 저장되는데, 이는 LangChain에서 사용자 메시지를 내부적으로 `HumanMessage` 클래스로 표현하기 때문입니다. LangChain의 메시지 객체는 `type` 속성으로 메시지의 실제 클래스명을 반환하므로, 메서드 이름과 타입 값이 다를 수 있다는 점을 기억하세요."
  },
  "q2548": {
    "B": "`add_user_message()` 메서드는 내부적으로 HumanMessage 객체를 생성하여 저장하므로, `history.messages[0]`은 실제로 HumanMessage의 인스턴스이며 `isinstance()` 검사 결과는 False가 아닌 True를 반환합니다. HumanMessage, AIMessage 등은 langchain에서 대화 메시지를 타입별로 구분하기 위해 정의된 클래스들이므로, 각 메서드가 자동으로 적절한 클래스의 객체를 생성한다는 점을 기억해 두세요!",
    "C": "isinstance() 함수는 객체의 타입을 검사하여 True 또는 False 중 하나를 반환하는데, None을 반환하지 않습니다. 이 코드에서 history.messages[0]은 add_user_message()로 추가된 HumanMessage 객체이므로 isinstance(history.messages[0], HumanMessage)는 True를 출력합니다. isinstance() 함수는 항상 불린 값(True/False)만 반환하는 타입 검사 함수라는 점을 기억하세요!",
    "D": "이 코드는 langchain_core 라이브러리의 표준 메서드들을 정확히 사용하고 있어서 오류 없이 정상 실행되며, `history.messages[0]`은 `add_user_message()`로 추가된 첫 번째 메시지인 HumanMessage 객체를 가리키므로 `isinstance()` 검사 결과 True가 출력됩니다. InMemoryChatMessageHistory는 메시지 저장을 위해 설계된 클래스로, 메서드 호출과 인덱싱 접근이 모두 정상적으로 동작하니 걱정하지 마세요!"
  },
  "q2550": {
    "A": "add_user_message와 add_ai_message로 메시지 2개를 추가했지만, clear() 메서드가 실행되면서 저장된 모든 메시지가 삭제되기 때문에 len(history.messages)는 2가 아닌 0을 반환합니다. clear() 메서드는 채팅 기록을 완전히 초기화하는 함수이니 이전의 메시지 개수만 생각하지 말고 각 메서드가 실행되는 순서를 따라가며 최종 상태를 파악해보세요!",
    "B": "history.clear() 메서드는 대화 기록에 저장된 모든 메시지를 삭제하므로, clear() 호출 후 messages 리스트는 비어있게 되어 len(history.messages)는 2가 아닌 0을 반환합니다. InMemoryChatMessageHistory의 clear() 메서드는 add_user_message()와 add_ai_message()로 추가된 모든 메시지를 완전히 제거하는 기능을 수행합니다.",
    "D": "InMemoryChatMessageHistory는 langchain_core에서 제공하는 표준 클래스이므로 정상적으로 작동하며, clear() 메서드도 유효한 메서드로서 오류 없이 히스토리를 완전히 비워줍니다. 메시지를 추가한 후 clear()로 모두 제거하면 messages 리스트가 비어있게 되어 len()은 0을 반환하므로 오류가 발생하지 않습니다."
  },
  "q2552": {
    "A": "`messages[1]`은 리스트의 두 번째 요소인 `AIMessage(content=\"반가워요\")`를 참조하는데, 첫 번째 요소인 `HumanMessage`가 아니기 때문에 type이 \"human\"이 아닙니다. LangChain에서 `AIMessage`의 타입은 \"ai\"로 설정되므로, 메시지 객체의 type 속성은 해당 메시지 클래스의 종류를 문자열로 반환합니다.",
    "C": "코드에서 추가된 메시지는 HumanMessage와 AIMessage이므로 messages[1]은 AIMessage 객체이며, AIMessage의 type 속성은 \"system\"이 아니라 \"ai\"를 반환합니다. LangChain에서 system은 SystemMessage라는 별도의 메시지 클래스가 있으므로, 각 메시지 타입은 해당 클래스에 따라 정해지는 것입니다.",
    "D": "LangChain의 메시지 타입은 메시지 클래스의 이름을 소문자로 변환한 값이므로, AIMessage 인스턴스의 type 속성은 'ai'이지 'assistant'가 아닙니다. 일반적인 LLM API에서 'assistant' 역할이 사용되지만, LangChain은 자체적인 메시지 타입 체계를 가지고 있다는 점을 기억하면 좋습니다."
  },
  "q2554": {
    "A": "메시지 리스트에 add_messages()로 3개의 메시지 객체(HumanMessage 2개, AIMessage 1개)를 모두 추가했는데, 마지막 메시지만 카운트되었다고 생각하신 것 같습니다. 실제로는 add_messages()가 전달된 모든 메시지를 history.messages에 누적시키므로 len()으로 확인하면 3이 나옵니다. 리스트나 메시지 컨테이너에 여러 항목을 한 번에 추가할 때는 각 항목이 모두 포함되며, 길이는 누적된 전체 항목의 개수가 됩니다.",
    "B": "HumanMessage 2개와 AIMessage 1개가 모두 add_messages()로 추가되었는데, HumanMessage만 세는 실수를 하신 것 같아요. add_messages()는 메시지 종류와 상관없이 전달받은 모든 메시지를 history.messages 리스트에 추가하므로 총 3개의 메시지가 저장됩니다. 메시지 종류(Human, AI 등)와 관계없이 add_messages()에 전달된 모든 메시지 객체가 리스트에 누적된다는 점을 기억하면 좋겠습니다.",
    "D": "`add_messages()` 메서드에 전달된 리스트를 자세히 보면 HumanMessage 2개와 AIMessage 1개, 총 3개의 메시지 객체가 있는데, 이들이 각각 하나의 메시지로 추가되므로 4개가 아니라 3개입니다. `len(history.messages)`는 추가된 메시지 객체의 개수를 반환하며, 각 메시지는 하나의 단위로 계산됩니다."
  },
  "q2556": {
    "A": "인덱스 -1은 리스트의 마지막 요소를 의미하는데, 추가된 메시지들 중 가장 마지막에 추가된 것은 \"두 번째 질문\"이므로 history.messages[-1].content는 \"두 번째 질문\"을 반환합니다. 음수 인덱싱은 리스트의 끝에서부터 역순으로 접근하는 방식이므로, -1은 항상 마지막 요소를 가리킨다는 점을 기억하세요!",
    "B": "`history.messages[-1]`은 리스트의 마지막 요소를 가져오는데, 코드에서 추가된 메시지의 순서를 보면 HumanMessage(\"첫 번째 질문\"), AIMessage(\"첫 번째 답변\"), HumanMessage(\"두 번째 질문\") 순이므로 마지막 요소는 \"두 번째 질문\"을 담은 HumanMessage입니다. 리스트의 인덱싱에서 음수 인덱스는 뒤에서부터 세므로 -1은 항상 맨 마지막 요소를 의미한다는 점을 기억하세요!",
    "D": "history.messages 리스트에 총 3개의 메시지가 저장되어 있으므로 [-1]은 마지막 요소인 HumanMessage 객체를 가리키며, 이 객체의 content 속성값은 \"두 번째 질문\"입니다. 파이썬 리스트의 인덱싱에서 음수 인덱스는 뒤에서부터 세어 실제 요소에 접근하므로 None이 아닌 문자열 값을 반환합니다."
  },
  "q2558": {
    "B": "리스트에 추가한 두 메시지 중 인덱스 1번째 요소는 AIMessage(content=\"반가워요\")이므로 isinstance(history.messages[1], AIMessage)는 True를 반환하는데, False라고 생각하신 것 같습니다. 파이썬의 isinstance() 함수는 객체가 특정 클래스의 인스턴스인지 확인해서 True 또는 False를 반환하므로, 실제로 AIMessage 타입인 객체를 확인하면 True가 나온다는 점을 기억해두세요.",
    "C": "isinstance() 함수는 객체가 특정 클래스의 인스턴스인지를 판단하여 True 또는 False의 불린 값을 반환하므로, None을 출력할 수 없습니다. 코드에서 history.messages[1]은 AIMessage 객체이기 때문에 isinstance(history.messages[1], AIMessage)는 True를 반환합니다. isinstance() 함수는 항상 True 또는 False 중 하나의 불린 값만 반환한다는 점을 기억해두세요.",
    "D": "이 코드는 LangChain의 공식 인메모리 채팅 히스토리 기능을 사용하고 있으며, add_messages() 메서드와 messages 속성이 정상적으로 작동하므로 오류가 발생하지 않습니다. history.messages[1]은 두 번째로 추가된 AIMessage 객체를 올바르게 참조하고, isinstance() 함수로 타입 검사를 수행하면 True를 반환합니다. LangChain 라이브러리의 메시지 저장소는 메서드와 속성이 안정적으로 구현되어 있어서, 올바른 형식의 메시지 객체를 전달하면 정상 작동한다는 점을 기억하세요!"
  },
  "q2560": {
    "A": "messages 리스트는 인덱스 0부터 시작하므로 messages[3]은 네 번째 메시지이며, 추가 순서대로 보면 Q1(0), A1(1), Q2(2), A2(3)이 되어 messages[3]은 add_ai_message로 추가된 \"A2\"입니다. AI 메시지의 type 속성은 \"ai\" 문자열을 반환하므로 \"human\"이 아닙니다. 파이썬에서 리스트는 0부터 인덱싱되며, 각 메시지 객체의 type 속성은 메시지 추가 방식에 따라 \"human\" 또는 \"ai\" 값을 가집니다.",
    "C": "system 타입의 메시지는 시스템 역할을 하는 메시지를 나타내는데, 이 코드에서는 add_user_message와 add_ai_message만 사용하여 human과 ai 메시지만 추가되므로 messages[3]은 네 번째 메시지인 ai 타입의 \"A2\"입니다. 채팅 히스토리에서 메시지의 타입은 추가된 메시지의 역할에 따라 결정되며, add_ai_message로 추가된 메시지는 항상 'ai' 타입을 가집니다.",
    "D": "InMemoryChatMessageHistory는 langchain_core에서 제공하는 실제 클래스로, add_user_message()와 add_ai_message() 메서드를 통해 메시지를 정상적으로 저장하고 messages 리스트로 접근할 수 있으므로 코드는 오류 없이 실행됩니다. 메시지는 추가된 순서대로 인덱스 0부터 저장되므로 인덱스 3은 네 번째로 추가된 \"A2\"(AI 메시지)이며, 그 type 속성값은 \"ai\"입니다."
  },
  "q2562": {
    "B": "Document 객체에서 page_content 속성은 문서의 실제 내용을 담고 있는데, metadata 딕셔너리가 아니라 \"RAG는 검색 증강 생성입니다.\"라는 문자열을 저장하고 있기 때문입니다. metadata 속성에 접근하면 {'source': 'textbook'}을 얻을 수 있지만, 코드는 page_content를 출력하고 있습니다. Document 객체에서 page_content와 metadata는 서로 다른 속성이므로 정확히 구분해서 사용해야 합니다.",
    "C": "이 오답은 `metadata` 딕셔너리의 값만 출력될 것이라고 착각한 것 같은데, 코드에서 접근하는 속성은 `doc.page_content`이지 `doc.metadata[\"source\"]`가 아니므로 \"textbook\"이 아닌 \"RAG는 검색 증강 생성입니다.\"가 출력됩니다. Document 객체에서 `page_content` 속성은 문서의 주요 내용을 담고 있으며, `metadata`는 별도로 저장되는 부가 정보라는 점을 기억하면 좋습니다.",
    "D": "Document 객체의 page_content 속성은 문서의 실제 내용을 저장하는 필드이므로 \"RAG는 검색 증강 생성입니다.\"라는 문자열 값을 가지며, None을 반환하지 않습니다. page_content는 Document 생성 시 명시적으로 전달한 값이 저장되므로, 항상 해당 문자열에 접근할 수 있습니다."
  },
  "q2564": {
    "B": "split_text() 메서드는 텍스트를 문자열 청크로 분할하는 메서드이기 때문에 chunks 리스트의 각 요소는 Document 객체가 아닌 str 타입입니다. Document 객체는 split_documents() 메서드를 사용할 때 반환되므로, 메서드의 차이를 구분하는 것이 중요합니다.",
    "C": "`split_text()` 메서드는 텍스트를 청크 단위로 나누어 문자열들의 리스트를 반환하므로, `chunks` 자체는 리스트이지만 `chunks[0]`은 그 리스트 안의 첫 번째 원소인 문자열입니다. 리스트의 개별 요소의 타입을 확인해야 하는 상황에서 전체 컨테이너의 타입을 고르는 실수를 했네요. `type()` 함수는 괄호 안의 객체 자체의 타입을 반환하므로, `chunks[0]`이라는 한 개의 문자열 원소를 검사할 때는 그 원소가 속한 자료형 하나를 골라야 합니다.",
    "D": "RecursiveCharacterTextSplitter의 split_text() 메서드는 텍스트를 문자열 리스트로 반환하기 때문에 chunks[0]은 딕셔너리가 아닌 문자열입니다. 만약 load_and_split() 메서드를 사용했다면 Document 객체가 반환되겠지만, split_text()는 순수 문자열만 리스트에 담아서 돌려줍니다."
  },
  "q2566": {
    "A": "Document 객체는 딕셔너리가 아닌 LangChain에서 정의한 클래스의 인스턴스이기 때문에, `type(doc).__name__`은 'dict'가 아니라 'Document'를 반환합니다. 객체의 타입은 그것이 어떤 클래스로 만들어졌는지를 나타내므로, Document 클래스로 생성된 인스턴스의 타입명은 항상 'Document'입니다.",
    "B": "`type(doc).__name__`은 doc 변수에 저장된 객체의 클래스 이름을 문자열로 반환하는 것이 아니라, 그 클래스 이름 자체를 나타내는 문자열을 출력하기 때문에 \"Document\"가 출력되는 것이지 str 타입 자체가 아닙니다. `type(doc)`는 Document 클래스를 가리키고, `.__name__`은 그 클래스의 이름인 \"Document\"를 문자열 형태로 나타낼 뿐 출력 결과는 Document라는 문자열 값입니다. `type()` 함수는 객체의 타입을 반환하므로, 이 코드의 결과는 Document 클래스 자체를 가리키고 있습니다.",
    "D": "Document 클래스는 BaseDocument를 상속받았지만, `type(doc).__name__`은 인스턴스의 실제 클래스명을 반환하므로 \"BaseDocument\"가 아닌 \"Document\"를 출력합니다. 상속 관계와 실제 인스턴스의 타입은 다르다는 점을 기억하면서, `type()` 함수는 항상 객체가 직접 생성된 클래스의 이름을 알려준다고 생각하세요."
  },
  "q2568": {
    "A": "`create_documents()` 메서드는 문자열을 분할한 후 Document 객체로 변환하여 반환하므로, `docs[0]`은 문자열이 아니라 LangChain의 Document 클래스 인스턴스입니다. 만약 단순히 문자열만 반환했다면 `split_text()` 메서드를 사용했을 것인데, `create_documents()`는 메타데이터를 포함한 구조화된 Document 객체를 만드는 것이 목적입니다.",
    "B": "`create_documents()` 메서드는 문자열 리스트를 받아서 처리하지만, 반환값은 Document 객체들의 리스트이므로 `docs[0]`은 리스트가 아니라 개별 Document 객체입니다. LangChain의 `create_documents()` 메서드는 텍스트를 청크로 분할한 후 각각을 Document 클래스의 인스턴스로 변환하여 반환합니다.",
    "C": "`create_documents()` 메서드는 딕셔너리 형태의 데이터를 반환하는 것이 아니라 LangChain의 Document 객체들을 담은 리스트를 반환하므로, `docs[0]`은 딕셔너리가 아닙니다. LangChain의 `create_documents()` 메서드는 텍스트를 자동으로 Document 객체로 변환해주는 편의 메서드입니다."
  },
  "q2570": {
    "A": "리스트의 인덱싱은 0부터 시작하므로 docs_list[1]은 두 번째 요소를 가리키는데, 보기 A는 인덱스 0에 해당하는 첫 번째 문서의 내용입니다. 파이썬의 0-based 인덱싱을 정확히 이해하면 docs_list[0]은 첫 번째, docs_list[1]은 두 번째 요소라는 점을 명확히 할 수 있습니다.",
    "C": "리스트의 인덱싱은 0부터 시작하므로 docs_list[1]은 두 번째 요소인 \"두 번째 문서\"를 가리키며, docs_list[2]일 때 세 번째 문서에 접근할 수 있습니다. 파이썬에서 리스트나 문자열 등의 시퀀스 자료형은 항상 0부터 시작하는 0-based 인덱싱을 사용한다는 점을 기억하세요!",
    "D": "Document 객체의 page_content는 초기화할 때 전달한 문자열 값을 그대로 저장하므로, docs_list[1]은 \"두 번째 문서\"라는 실제 내용을 가지고 있으며 None이 아닙니다. 파이썬의 리스트 인덱싱에서 [1]은 두 번째 요소를 의미하고, 해당 Document 객체는 생성될 때 전달받은 문자열을 속성으로 보관하고 있다는 점을 기억해봅시다."
  },
  "q2572": {
    "A": "page_content는 Document의 본문 내용을 저장하는 속성이고, metadata[\"source\"]는 메타데이터 딕셔너리에서 \"source\" 키의 값을 접근하는 것이므로 \"LangChain 소개\"가 아닌 \"wiki\"가 출력됩니다. Document 객체에서 page_content와 metadata는 서로 다른 속성이며, 각각 다른 데이터를 저장한다는 점을 기억하세요!",
    "C": "Document 객체의 metadata는 딕셔너리 형태이고, `doc.metadata[\"source\"]`는 딕셔너리에서 \"source\" 키의 값을 직접 접근하는 것이므로 \"wiki\" 문자열만 반환됩니다. 만약 `doc.metadata` 전체를 출력했다면 {'source': 'wiki'}가 나왔겠지만, 여기서는 대괄호를 사용해 특정 키의 값을 꺼내고 있습니다. 딕셔너리에서 특정 키로 접근할 때는 그 키에 해당하는 값만 반환된다는 점을 기억하세요!",
    "D": "Document 객체를 생성할 때 metadata 딕셔너리에 \"source\": \"wiki\"가 명시적으로 저장되었으므로, doc.metadata[\"source\"]에 접근하면 None이 아니라 실제 값인 \"wiki\"가 반환됩니다. Document의 metadata 속성은 생성자로 전달된 딕셔너리를 그대로 저장하기 때문에, 지정된 키에 대응하는 값이 존재하면 그 값을 정확히 조회할 수 있습니다."
  },
  "q2574": {
    "A": "RecursiveCharacterTextSplitter는 chunk_size=20으로 설정되었을 때 텍스트를 20자 단위로 나누기 때문에, \"LangChain은 LLM 앱 프레임워크입니다. RAG는 검색 증강 생성입니다.\"라는 45자 문장이 여러 개의 청크로 분할되어 3개의 문서가 생성됩니다. 텍스트 분할기는 전체 텍스트를 한 덩어리로 두지 않고 설정된 크기에 맞춰 자동으로 여러 조각으로 나누는 것이 핵심입니다.",
    "B": "RecursiveCharacterTextSplitter는 chunk_size=20에 맞춰 텍스트를 나누는데, 문장 부호와 띄어쓰기를 기준으로 재귀적으로 분할하기 때문에 단순히 2개의 문장으로 나뉘지 않습니다. 실제로는 \"LangChain은 LLM 앱\", \"프레임워크입니다.\", \"RAG는 검색 증강\", \"생성입니다.\" 총 3개의 청크로 분할되므로 len(docs)는 3이 됩니다. RecursiveCharacterTextSplitter는 구분자 목록([\"\\n\\n\", \"\\n\", \" \", \"\"])을 순서대로 적용하여 청크 크기를 초과하지 않으면서도 의미 있는 단위로 텍스트를 분할합니다.",
    "D": "RecursiveCharacterTextSplitter는 chunk_size=20으로 설정되어 있으므로 각 청크가 최대 20자를 넘지 않도록 분할하는데, 주어진 문장을 실제로 실행하면 \"LangChain은 LLM 앱\", \"프레임워크입니다.\", \"RAG는 검색 증강 생성입니다.\"의 3개 청크로 분할되므로 4가 아닙니다. 텍스트 분할기는 chunk_size 제한에 맞춰 문장 부호나 공백 같은 구분자를 기준으로 재귀적으로 분할하기 때문에, 단순히 20으로 나눈 개수보다는 실제 분할 결과를 확인해야 합니다."
  },
  "q2576": {
    "B": "RecursiveCharacterTextSplitter는 chunk_size=20으로 설정되어 있기 때문에 한 청크는 최대 20글자까지만 포함되는데, \"LangChain은 LLM 앱 프레임워크입니다.\"는 20글자를 초과하므로 더 작은 단위로 분할되어 첫 번째 청크는 \"LangChain은 LLM 앱\"(18글자)이 됩니다. 텍스트 분할기는 지정된 크기 제한을 엄격하게 지키기 때문에 전체 문장이 아닌 크기 내에서 의미 있는 단위로 나뉜다는 점을 기억하세요.",
    "C": "RecursiveCharacterTextSplitter는 텍스트를 chunk_size(20)에 맞게 앞에서부터 순서대로 분할하므로, docs[0]은 첫 번째 청크인 \"LangChain은 LLM 앱\"이지 문자열 전체에서 \"RAG는 검색 증강 생성입니다.\"를 찾아내는 것이 아닙니다. 텍스트 분할기는 입력된 문서를 지정된 크기의 청크로 순차적으로 나누며, 인덱스 0은 항상 맨 처음 청크를 가리킵니다.",
    "D": "create_documents() 메서드는 입력 텍스트를 chunk_size에 따라 분할하여 Document 객체의 리스트를 반환하므로, docs[0]은 None이 아니라 첫 번째 청크인 Document 객체이며 page_content 속성으로 접근하면 \"LangChain은 LLM 앱\"이라는 문자열을 얻게 됩니다. RecursiveCharacterTextSplitter는 텍스트를 실제로 분할 처리하는 도구이므로, 메서드가 정상 작동하면 항상 Document 객체들을 포함한 리스트를 반환한다는 점을 기억하세요."
  },
  "q2578": {
    "A": "문자열 \"1234567890\" * 2는 총 20자이고, chunk_size=10으로 설정되었으므로 10자씩 2개의 청크로 나뉘게 되는데, 1개라고 생각하신 것 같습니다. RecursiveCharacterTextSplitter는 지정된 chunk_size를 초과하는 텍스트를 chunk_overlap을 고려하여 여러 개의 청크로 분할하므로 이 경우 2개의 청크가 생성됩니다. 텍스트 분할기는 입력 길이가 chunk_size보다 크면 반드시 여러 청크로 나뉜다는 점을 기억하세요!",
    "C": "RecursiveCharacterTextSplitter는 chunk_size=10일 때 정확히 10자씩 나누므로, \"1234567890\" * 2는 20자이고 이를 2개의 청크로 분할하는데, 3개로 세신 것은 overlap을 고려할 때 생기는 추가 청크가 있을 거라 예상하신 것 같습니다. 하지만 chunk_overlap=0으로 설정했으므로 중복 없이 정확히 2개의 균등한 청크만 생성됩니다.",
    "D": "RecursiveCharacterTextSplitter는 chunk_size=10으로 설정되었을 때 정확히 10자씩 나누므로, \"1234567890\"을 2번 반복한 20자 문자열은 10자씩 2개의 청크로 분할되는데 4개라고 생각하신 것 같습니다. 청크 크기와 문자열 길이의 관계를 다시 계산해보면 20 ÷ 10 = 2개의 청크가 생성된다는 것을 확인할 수 있습니다."
  },
  "q2580": {
    "A": "Document의 metadata 딕셔너리에서 \"source\" 키의 값을 접근하는 것이므로 page_content가 아니라 메타데이터 값인 \"wiki\"를 반환합니다. metadata[\"source\"]는 문서의 내용이 아닌 문서의 속성 정보를 조회하는 것입니다.",
    "B": "Document의 metadata는 딕셔너리 형태이지만, `metadata[\"source\"]`처럼 특정 키로 접근하면 값 자체인 문자열 'wiki'를 반환하지, 딕셔너리 전체를 반환하지 않습니다. 딕셔너리의 키로 접근할 때와 딕셔너리 자체를 출력할 때의 차이를 구분하는 것이 중요합니다.",
    "D": "split_documents() 메서드는 문서를 분할할 때 원본 Document의 메타데이터를 함께 유지하므로, split_docs[0].metadata는 {\"source\": \"wiki\"}가 되어 None이 아닙니다. 메타데이터 키에 접근할 때는 딕셔너리처럼 접근하면 해당 값을 얻을 수 있다는 점을 기억하세요!"
  },
  "q2582": {
    "A": "RecursiveCharacterTextSplitter는 여러 Document 객체를 입력받을 때 각 문서를 독립적으로 분할하므로, 2개의 문서가 각각 100자 이상 분할되지 않으면 그대로 유지되어 총 2개의 결과를 반환합니다. 이 코드에서 \"문서A\"와 \"문서B\"는 각각 3자 미만이므로 분할되지 않고, 결과적으로 2개의 Document 객체가 유지됩니다.",
    "C": "RecursiveCharacterTextSplitter는 각 Document 객체를 독립적으로 처리하기 때문에, \"문서A\"와 \"문서B\" 두 개의 Document가 입력되면 두 개의 Document가 그대로 출력됩니다. chunk_size=100은 각 문서의 내용이 100자를 초과할 때만 분할하는데, \"문서A\"와 \"문서B\"는 매우 짧아서 분할되지 않으므로 3개가 아닌 2개의 결과가 나옵니다. split_documents()는 입력된 Document 객체의 개수만큼 출력하며, 각 Document는 chunk_size보다 작으면 분할되지 않습니다.",
    "D": "RecursiveCharacterTextSplitter는 각 Document를 독립적으로 처리하므로, 문서A와 문서B가 각각 하나씩 청크로 분할되어 총 2개의 Document가 반환됩니다. 청크 크기가 100이고 각 문서의 내용이 \"문서A\"와 \"문서B\" 정도로 짧아서 추가로 분할되지 않기 때문에 4개가 아닌 2개의 결과만 얻게 됩니다. split_documents() 메서드는 입력된 Document 객체의 개수를 기준으로 처리하며, chunk_size는 텍스트 길이에 따른 분할 여부를 결정할 뿐 Document 개수를 증가시키지 않습니다."
  },
  "q2584": {
    "B": "StrOutputParser의 invoke 메서드는 AIMessage 객체를 받아서 그 안의 content 문자열을 추출하므로, result에는 \"LangChain 종합실습\"이라는 문자열이 저장되어 isinstance(result, str)은 True를 반환합니다. StrOutputParser는 LangChain의 출력 파서로서 메시지 객체에서 문자열 콘텐츠를 추출하는 것이 정확한 역할이라는 점을 기억하세요.",
    "C": "StrOutputParser는 AIMessage 객체를 처리할 때 메시지의 content 속성값인 문자열을 추출하여 반환하므로 None이 아니라 \"LangChain 종합실습\"이라는 문자열을 반환합니다. parser.invoke()의 반환값이 문자열이므로 isinstance(result, str)은 True를 출력하게 되는 것이죠. 출력 파서의 역할은 LLM의 응답을 원하는 형식으로 변환하는 것이며, StrOutputParser는 문자열 형식으로의 변환을 담당합니다.",
    "D": "StrOutputParser는 AIMessage 객체를 문자열로 변환하도록 설계되었기 때문에 content 속성값인 \"LangChain 종합실습\"을 안전하게 추출하여 문자열로 반환하며 오류가 발생하지 않습니다. LangChain의 파서는 특정 메시지 타입을 입력받아 처리하는 것이 정상 동작이므로, AIMessage를 invoke() 메서드에 전달하는 것은 완전히 올바른 사용법입니다."
  },
  "q2586": {
    "A": "page_content는 문서의 본문 내용을 저장하는 속성이고, metadata[\"chapter\"]는 메타데이터 딕셔너리에서 \"chapter\" 키의 값을 접근하는 것이므로 \"LangChain RAG 파이프라인\"이 아닌 5가 출력됩니다. Document 객체에서 실제 데이터에 접근할 때는 어떤 속성을 조회하는지 정확히 파악하는 것이 중요합니다.",
    "B": "metadata 속성 자체를 출력하려고 하셨나요? docs[0].metadata[\"chapter\"]는 딕셔너리 전체가 아니라 \"chapter\" 키에 해당하는 값만 접근하는 것이므로 5가 출력됩니다. 딕셔너리에서 특정 키의 값을 추출할 때는 [키] 문법을 사용하면 그 값만 반환된다는 점을 기억해두세요.",
    "D": "RecursiveCharacterTextSplitter로 문서를 분할할 때 메타데이터는 원본 문서의 메타데이터가 그대로 유지되므로, docs[0].metadata[\"chapter\"]는 원본의 chapter 값인 5를 반환합니다. 문서 분할 과정에서 메타데이터가 손실되지 않는다는 점을 기억하면 이 문제를 쉽게 풀 수 있습니다."
  },
  "q2588": {
    "A": "당신은 `{role}`과 `{followup}` 두 개의 변수만 세었을 것 같은데, `{question}`도 중괄호로 감싸진 템플릿 변수이기 때문에 총 3개가 됩니다. ChatPromptTemplate의 input_variables는 \"ai\" 역할의 메시지처럼 고정된 내용은 제외하고 중괄호 안의 동적 변수만 카운트합니다.",
    "C": "코드에서 중괄호로 감싼 변수는 {role}, {question}, {followup} 3개인데, 이 중 \"ai\" 메시지의 \"이해했습니다.\"에는 변수가 없으므로 실제 입력 변수는 3개입니다. ChatPromptTemplate의 input_variables는 템플릿에서 실제로 값을 받아야 하는 변수만 세기 때문에 메시지의 개수가 아니라 중괄호 변수의 개수를 기준으로 합니다.",
    "D": "ChatPromptTemplate에서 입력 변수는 중괄호로 감싼 {role}, {question}, {followup} 세 개이며, \"ai\" 역할의 메시지는 고정된 응답이라 입력 변수로 포함되지 않습니다. 프롬프트 템플릿의 입력 변수는 런타임에 값을 받아야 하는 플레이스홀더만 세어진다는 점을 기억해보세요!"
  },
  "q2590": {
    "A": "리스트 컴프리헨션으로 split_docs의 각 Document 객체마다 HumanMessage를 생성하기 때문에, 원본 문서 2개가 청크 크기 100보다 작아서 분할되지 않으면 그대로 2개의 메시지가 만들어집니다. 메시지 리스트의 길이는 생성된 HumanMessage 객체의 개수를 의미하므로 1이 아니라 2입니다. 리스트 컴프리헨션은 원본 리스트의 각 요소마다 새로운 요소를 하나씩 생성하므로, 입력 리스트의 길이와 출력 리스트의 길이가 같다는 점을 기억하세요.",
    "C": "각 Document의 page_content가 100자보다 짧아서 RecursiveCharacterTextSplitter에 의해 분할되지 않고, 그 결과 split_docs는 원본과 동일한 2개의 Document로 유지되기 때문에 3이 아닙니다. 리스트 컴프리헨션으로 생성되는 HumanMessage는 split_docs의 길이와 정확히 같으므로, history.messages에는 2개의 메시지만 저장됩니다.",
    "D": "history 객체에 add_messages()로 메시지를 추가했으므로 history.messages는 비어있지 않으며, split_docs에는 \"문서1\"과 \"문서2\" 두 개의 Document가 있어서 2개의 HumanMessage가 추가됩니다. InMemoryChatMessageHistory는 add_messages() 메서드로 추가된 메시지들을 messages 속성에 저장하므로, 실제로는 len(history.messages)는 2를 반환합니다."
  },
  "q2592": {
    "A": "프롬프트 템플릿에는 \"role\"과 \"input\" 두 개의 변수가 중괄호로 표시되어 있는데, A번 선택지는 이 중 하나만 세어서 input_variables의 길이를 1로 잘못 파악하신 것 같습니다. ChatPromptTemplate의 input_variables는 템플릿 전체에서 중괄호로 감싸진 모든 변수를 자동으로 수집하므로 \"role\"과 \"input\" 둘 다 포함되어 2개가 됩니다. 템플릿에 사용된 플레이스홀더 개수를 세는 연습을 통해 변수 추출 원리를 확실히 이해해 보세요!",
    "C": "ChatPromptTemplate의 input_variables는 템플릿에 포함된 중괄호 변수들을 자동으로 수집하는데, 이 코드에서는 \"당신은 {role}입니다.\"와 \"{input}\" 두 곳에만 변수가 있으므로 role과 input 2개만 카운트됩니다. ChatPromptTemplate은 system, human 같은 메시지 역할 자체는 변수로 인식하지 않고 중괄호로 감싼 동적 값만 input_variables에 포함시킵니다.",
    "D": "ChatPromptTemplate에서 input_variables는 템플릿에 포함된 모든 중괄호 변수를 추적하는데, 이 코드에서는 \"당신은 {role}입니다\"의 role과 \"{input}\"의 input 두 개 변수가 있으므로 길이는 0이 아니라 2입니다. input_variables는 동적으로 채워야 할 플레이스홀더를 의미하므로, 빈 리스트가 아닌 이상 0이 될 수 없다는 점을 기억하세요."
  },
  "q2594": {
    "A": "문자열 \"ABCDEFGHIJKLMNOPQRST\"는 20글자이고, chunk_size가 10이므로 10글자씩 두 번에 나누어져 \"ABCDEFGHIJ\"와 \"KLMNOPQRST\" 두 개의 청크가 생기는데, 이를 1개로 세신 것 같습니다. RecursiveCharacterTextSplitter는 지정된 크기 이상의 텍스트를 여러 청크로 분할하므로 len(chunks)의 결과는 2입니다.",
    "C": "RecursiveCharacterTextSplitter는 chunk_size=10으로 설정되었을 때 텍스트를 최대 10글자씩 나누므로, 20글자인 \"ABCDEFGHIJKLMNOPQRST\"는 \"ABCDEFGHIJ\"와 \"KLMNOPQRST\" 두 개의 청크로만 분할되어 4가 아닌 2가 됩니다. chunk_size는 각 청크의 최대 길이를 의미하며, 텍스트의 글자 수를 나누는 것이 아니라 지정된 크기만큼 순차적으로 분할하는 방식이라는 점을 기억하세요.",
    "D": "RecursiveCharacterTextSplitter는 주어진 텍스트를 chunk_size 크기로 나누므로, 20자의 문자열을 chunk_size=10으로 설정하면 \"ABCDEFGHIJ\"와 \"KLMNOPQRST\" 두 개의 청크가 생성되어 len(chunks)는 0이 아닌 2가 됩니다. 텍스트 분할기는 입력 텍스트가 있으면 항상 최소 1개 이상의 청크를 반환하므로, 결과가 0이 될 수는 없습니다."
  },
  "q2596": {
    "B": "clear() 메서드를 호출하면 저장된 모든 메시지가 초기화되므로, clear() 이후에 \"새 질문\"만 추가되어 총 메시지 개수는 2개가 아니라 1개입니다. InMemoryChatMessageHistory의 clear()는 메시지 전체를 삭제하는 메서드이므로, 이전에 저장된 Q1, A1, Q2, A2는 모두 제거됩니다.",
    "C": "clear() 메서드는 이전 메시지들을 삭제하지만, 그 이후에 add_user_message(\"새 질문\")으로 새로운 메시지를 추가했기 때문에 messages 리스트는 비어있지 않고 1개의 메시지를 포함하게 됩니다. 메서드 호출의 순서를 정확히 파악하면, 삭제 후에도 새로운 데이터가 추가되므로 최종 메시지 개수는 1개가 됩니다.",
    "D": "clear() 메서드를 호출하면 이전의 모든 메시지(Q1, A1, Q2, A2)가 완전히 삭제되기 때문에, clear() 이후에 추가된 \"새 질문\" 메시지만 남아있어 4가 아니라 1이 됩니다. clear()는 채팅 히스토리를 초기화하는 메서드로, 이후 새로 추가되는 메시지부터만 카운트된다는 점을 기억하세요!"
  },
  "q2598": {
    "A": "`from_messages`에 전달된 튜플이 4개이므로 `format_messages` 메서드는 각 튜플을 하나의 메시지 객체로 변환하여 총 4개의 메시지를 생성합니다. 2를 선택하신 것은 변수가 할당된 `{role}`과 `{question}`만 세신 것 같은데, 실제로는 \"system\", \"human\", \"ai\", \"human\" 4가지 역할의 메시지가 모두 리스트에 포함됩니다. ChatPromptTemplate의 `format_messages`는 템플릿의 모든 메시지 튜플을 메시지 객체로 변환하므로, 변수 개수가 아닌 정의된 메시지의 개수를 세어야 합니다.",
    "B": "ChatPromptTemplate.from_messages()에 전달한 튜플이 4개(system, human, ai, human)이므로 3개라고 생각하실 수 있지만, format_messages()는 각 튜플을 하나의 메시지 객체로 변환하기 때문에 실제 메시지 개수는 4개입니다. ChatPromptTemplate에서 from_messages()로 정의한 메시지 튜플의 개수가 곧 format_messages() 호출 후 반환되는 메시지 리스트의 길이가 됩니다.",
    "D": "ChatPromptTemplate.from_messages()에 전달된 튜플은 4개이고, 각 튜플이 하나의 메시지 객체로 변환되므로 msgs 리스트의 길이는 4개입니다. {role}, {question}, {followup} 같은 변수의 개수를 센 것이 아니라 실제 메시지 객체의 개수를 세어야 합니다. LangChain의 ChatPromptTemplate는 from_messages()에 입력된 각 (role, content) 튜플을 독립적인 메시지 객체로 변환하므로, 변수 개수가 아닌 튜플의 개수만큼 리스트 길이가 결정됩니다."
  },
  "q2600": {
    "B": "AIMessage가 출력될 것이라고 생각했다면, 코드의 `history.clear()`에서 모든 메시지가 삭제되고 `add_user_message(\"메모리란?\")`로 새로 추가되는 과정을 놓친 것입니다. 따라서 `history.messages[0]`은 가장 최근에 추가된 유일한 메시지인 HumanMessage이므로 그 type은 \"human\"입니다.",
    "C": "history.clear()로 모든 메시지가 삭제된 후 add_user_message(\"메모리란?\")로만 메시지가 추가되었기 때문에, messages[0]은 system 타입이 아니라 사용자가 입력한 human 타입의 메시지입니다. 메시지 type 속성은 각 메시지 객체의 실제 클래스에 따라 결정되며, HumanMessage는 항상 \"human\" 타입을 반환합니다.",
    "D": "`history.clear()`로 모든 메시지를 삭제한 후 `add_user_message(\"메모리란?\")`으로 새로운 메시지를 추가했으므로 `history.messages[0]`은 유효하게 존재하며, 이 메시지의 type 속성에 접근할 수 있어 오류가 발생하지 않습니다. InMemoryChatMessageHistory는 메시지를 저장하고 조회하는 것이 정상 동작이므로, 코드는 문제없이 실행되어 \"human\"을 출력합니다."
  }
}